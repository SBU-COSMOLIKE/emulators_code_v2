# Memory index

- [Session status 2026-07-07b (read FIRST to resume)](session-status-2026-07-07b.md) — compaction-4 snapshot: NINE audited commits since 8bb5484 (2d2f68d doc-audit -> ebd9869 freeze_trunk -> a95f8df head-activation -> ddb6c98 precedence appendix -> fc4655a YAML chapter -> 29b23dd two-README split + parallel/ deleted -> 83a1e58 model-section polish -> 8ad25a1 relu/tanh+norm -> 75c429e overnight didactic+weight-decay -> 378dc95 NPCE). IN FLIGHT uncommitted: save-schema-v2 CORE + riders 1-5, ACCEPTED WITH TWO DELTAS (D-SV1 compile_mode shared constant; D-SV2 the eval_bs stash) awaiting the micro-fix, then ONE combined commit (exclude compute_data_vectors/, staged for its own unit). QUEUE: generator import + appendix, cobaya adapter (either order, both handoff-ready) -> the generate-train-save-sample arc completes. Standing rules born: artifacts-never-trust-defaults (3-leg doctrine), the unified GitHub-math rule (5-rule scanner), subsection-code-blocks, note-is-the-spec-of-record, valid-tree-STOP commended. Workstation board: GM-C FIRST @46ec5e1 + standing gates + GFT-C/GHA-F/GAN-C/GWD-C/GPC-C/GSV-C (+GCT-C later). Architect harness-bug pattern owned: verify the harness on a known-good case first.
- [compute_data_vectors/ import + the training-set appendix (spec, staged)](compute-data-vectors-import.md) — SPEC 2026-07-07 (user: the README needs the how-parameters-are-set logic + a compute_data_vectors folder for the generator): dataset_generator_lensing.py STAGED VERBATIM in the tree (1121 lines, py_compile-clean; import rule binding — byte-identical except the header path examples emultrf/emultraining -> emultrfv2/compute_data_vectors; battle-tested MPI code, NO style retrofit). NEW README appendix "Generating the training set" (before AI-Usage which stays last): TWO SAMPLING MODES as peers — tempered (--unif 0: log p_T = [-(1/2) y^T Sigma-tilde^-1 y + log pi]/T) and UNIFORM (--unif 1: flat in the stretched bounds; still needs --temp, which sets the hard-boundary stretch for Gaussian/unbounded priors; files tag _unifs not _<T>; use for coverage/stress studies) — with the why-lines (T widens the cloud past the posterior; maxcorr 0.15 fills degeneracy-perpendicular volume; --boundary < 1 keeps val/test inside the support), emcee/MPI/checkpoint machinery in one paragraph, THE OUTPUT CONTRACT table (.1.txt columns = the staging slice; .paramnames = the naming loop; .covmat = train_covmat = the whitening basis; the .npy dump), and the LOUD two-train_args disambiguation (the generator's cobaya-YAML train_args is unrelated to the trainer's). Gates GDG-A (verbatim diff vs the staged copy) / GDG-B (docs+anchors) / GDG-C light (compile only; generation runs are science ops). Sequenced after save-schema-v2; independent of the cobaya adapter. IMPLEMENTED 2026-07-07 (Opus, base b2afca6), uncommitted: GDG-A verbatim diff = only the line-20 header path (emultrf/emultraining -> emultrfv2/compute_data_vectors) + generator py_compile clean; GDG-B docs+anchors+five-rule-math PASS (appendix = section 17, AI-Usage -> section 18 stays last; the two carried save-v2 docs landed); GDG-C py_compile PASS; the .py numerics verified against the script (temp*width/5 stretch, maxcorr 0.15, y = theta - fiducial, the _unifs tag, DEMove/DESnookerMove). __pycache__ not staged.

- [Cobaya Theory adapter (spec, HOLD behind save-schema-v2)](cobaya-theory-adapter.md) — SPEC 2026-07-07 (user: the MCMC Theory block; legacy files analyzed — its ord/extrapar/duplicated-emulator.py are ALL drift channels the standing rule kills): three layers — (1) EmulatorPredictor in emulator/inference.py (built on rebuild_emulator; owns ALL physics: saved-geometry encode -> forward -> the factored-IA combine (nla/tatt coeffs at inference!) -> the NPCE recombine -> decode; exposes .names); (2) cobaya_theory/emul_cosmic_shear.py = THIN adapter (name + likelihood contract get_cosmic_shear KEPT; requirements from the h5's stored names; ~100 lines, no nn.Module); (3) the MCMC YAML shrinks to {device, emulators: ['<path root>']} — no ord, no extrapar, the file IS the emulator. Decision points: dv shape (kept vs full) verified against the C likelihood + matched to legacy; TPU dropped; compile-at-inference off. Gates GCT-A/B Mac + GCT-C cocoa-env (THE PARITY PROBE: predictor vs training-side eval on probe points; the evaluate-run datavector compared end-to-end). Queue: NPCE (in flight) -> save-schema-v2 -> THIS. Install path = emulators/emultrfv2 (the new cocoa clone; no /dev). ADDENDUM: ord stays retired (h5 names = the ordering; sampled-vs-training name mismatches bridged by cobaya derived-param lambdas + renames — the example YAML ships the pattern); fast_params ADDED (user catch, Roman legacy shape): requirements passthrough for sampler-provided params that never enter the network (M calibration); in-theory M application = a flagged decision point verified against the likelihood at build. IMPLEMENTED 2026-07-07 (Opus, base a5dd04f), uncommitted: emulator/inference.py (EmulatorPredictor, the decoder reuses the exact training chi2fn.decode per branch), cobaya_theory/emul_cosmic_shear.py (thin, no nn.Module, whitelist rejects ord/extrapar), cobaya_theory/EXAMPLE_EMUL_EVALUATE.yaml (modernized from lsst_y1, lambda bridge kept). rebuild_emulator extended to a 4-tuple (info{ia,pce_base,pce_form} + compile_model flag). dv-shape DECLARED = kept entries (legacy res[0]). GCT-A/B PASS (numpy-stubbed torch + static); GCT-C parity probe rides the cocoa queue. Architect audit found + fixed a BLOCKING bug (D-CT1): rebuild_emulator hardcoded ParamGeometry.from_state, so a factored run's AmplitudeFactorGeometry could not rebuild — now save persists each geometry group's "cls" (materialized from type()) and rebuild dispatches on it (loud re-save error if the marker is absent; also fixes a latent LogParamGeometry mis-rebuild); + D-CT2 (YAML block-style emulators) + D-CT3 (PCEResidualChi2.decode docstring shape). COMMIT-READY, one combined commit. LAST unit of the generate->train->save->sample arc.
- [Save schema v2 + STANDING RULE: artifacts never trust defaults (spec, sequenced after NPCE)](save-schema-resolved-config.md) — STANDING RULE (user verbatim: "The philosophy over the emul and h5 file has to be — dont trust on default values — they can drift"): WRITE side = everything consumed is written, defaults MATERIALIZED (enforced by the GSV-A signature-vs-recipe census); READ side = loaders read ONLY the file, missing key = loud error, NEVER a code-default fallback (enforced by rebuild_emulator's hard rule + the GSV-C monkeypatched-drift test). The consumed-view doctrine's second half (displays RENDER it, artifacts PERSIST it, loaders TRUST ONLY it); every future save/load spec states how it honors both halves, every audit checks them. SPEC 2026-07-07 (user: the emul+h5 must reconstruct the emulator perfectly even if ALL code defaults drift): CONFIRMED GAP — the h5 saves config_yaml/train_args_yaml search-collapsed but NOT default-materialized (omitted norm/scheduler kwargs/n_gates/gate_init/compile_mode/freeze_trunk/berhu/ema/thresholds are resolved at build/run time and lost). FIX = the banner directive's second half: artifacts PERSIST the consumed view, by the same code that consumes — build_specs assembles exp.resolved_model (serializable rebuild recipe, factories by name); run_emulator returns resolved_train (every knob defaults-materialized + per-phase resolved + derived eval_bs; declared interface change); save v2 keeps the raw configs (provenance) + adds config_resolved_yaml + model_recipe/ + schema_version=2 + git-hash attr; NEW rebuild_emulator(path) reads ONLY the h5 (missing key = loud error, NEVER a code default; v1 files refused) and returns the inference triple. Acceptance: save->rebuild bitwise-equal on a probe batch AND the drift test (monkeypatched defaults, equality holds). Out of scope: training-resume checkpointing. RIDER 5: the UNIFIED math rule — GitHub eats backslash+ASCII-punctuation (\_ \\ \! \,) while backslash+letters survive; every math bug so far was ONE class; twelve \,/\! instances across nine lines enumerated + deleted (pure typography); the permanent scanner rule = flag any \<punct> in math, spacing via plain spaces or \quad/\qquad. RIDER 4: two more loss-section render fixes (the cases env's \\[4pt] separator arrives as literal text — replace with an environment-free one-line piecewise; '$k = $' edge-whitespace spans refuse to parse — equals moves outside the span) + two permanent policy additions (no \begin environments / row separators in GitHub math; no edge-whitespace inline spans; fences exempt). RIDER 3: the TATT display render fix (GitHub shreds $$ blocks whose continuation lines start with '+ ' — a bullet marker — into lists; fix = trailing operators; POLICY EXTENSION joins GRO-G: no $$ continuation line may start with +/-/*/>/digit-dot, the scanner checks it permanently; the one offender = README ~601, both files otherwise clean). RIDER 2: the .paramnames cross-check (the dumps' getdist sidecar declares cobaya names per .txt column — the training/covmat name chain closes the MCMC loop natively; at staging, cross-check sidecar vs covmat-header names when the sidecar exists, loud on mismatch, absent = no check). RIDER: the emultrfv2 install-path sweep (the repo re-clones as external_modules/code/emulators/emultrfv2, no /dev — fifteen doc-only sites: README D= line, code-map layout note, twelve driver-header comments; gate greps emultrf/dev to zero). Gates GSV-A..C (A = a programmatic signature-vs-recipe census so future knobs cannot skip the recipe). NOT implemented.
- [NPCE YAML wiring (spec, handoff ready)](npce-yaml-wiring.md) — SPEC 2026-07-07 (user: wire PCE first-class, "past failures on low T do not discourage me"): loss-space route — a NEW TOP-LEVEL pce: block (deliberately NOT in train_args: sweep_hyperparam builds geometry once, so a pce knob there would sweep without refitting = silent no-op; top-level = structurally unsweepable, one study per config). Schema = from_training verbatim ({form: residual|ratio required; p_max 4, r_max 2, q 0.5, k_max 40, loo_max 0.05, max_terms 30, max_fail 4}); validate_pce pure + EXCLUSIVITY (pce+rescale, pce+ia both error — each replaces chi2fn); build_geometry fits the base on the staged train set + wraps PCEResidualChi2/PCERatioChi2 (sweep_ntrain/bakeoff refit per N, correct); PCEEmulator gains state()/from_state + a pce h5 group in save_emulator; diagnostics plain-only gate extends; banner line + quiet-gated fit report; the refiner = any model.name with every current knob (the PCE losses already forward the loss-ladder kwargs; needs_params/batching plumbing already exist). PCEEmulator stays OUT of MODELS/DesignSpec (loss-owned). README gains a pce: chapter section + precedence rows + code-map update + YAML block. Gates GPC-A/B Mac + GPC-C workstation. Sequenced after the overnight commit. NOT implemented.

- [THE OVERNIGHT QUEUE 2026-07-07: BOTH UNITS IMPLEMENTED + Architect-verified, ONE COMBINED COMMIT READY](weight-decay-only-weight-matrices.md) — Executed overnight (Opus, base 8ad25a1), morning re-audit ACCEPTED no deltas (own harness: the real make_optimizer census 6/6 — decay exactly the Linear/Conv1d/BinLinear weights, multigate (K,dim) + BinLinear bias misclassifications FIXED, count conserved; unit 1: 16 command lines byte-identical, workstation mentions zero, six-entry vocab box, anchors/scan clean, drivers AST-identical). All five deviations ruled ACCEPTED — the section-6 adjacency call correct; two spec slips owned by the Architect (#9c stale location, Five-vs-Six header). Combined commit sentence in the note. GWD-C joins the workstation board (now GM-C FIRST at 46ec5e1, then the standing gates + GP-D/GH-E/GE-C/GB-C/GL-D/GBA-C/GME-C/item-27 + GFT-C + GHA-F + GAN-C + GWD-C). — user asleep, Implementer runs: ONE session, TWO units in order, ONE combined diff + ONE combined commit suggestion (purity traded for throughput, user-accepted). Precondition: the GAN commit landed (git log -1). Unit 1 = the README didactic pass (readme-run-it-definitions.md); unit 2 = the weight-decay allowlist fix + README section-6 treatment (SPEC FINALIZED in the note: decay = exactly the .weight of nn.Linear/Conv1d/BinLinear via named_modules, dedupe by id, signature/fused/two-group unchanged, docstring truth; gates GWD-A/B Mac + GWD-C workstation). Disjoint files except README (regions disjoint; collision = STOP). Morning: Implementer report -> Architect re-audit -> user commits + merges -> workstation sync -> the board (GM-C FIRST at 46ec5e1; + GFT-C, GHA-E, GHA-F, GAN-C, GWD-C...) -> the science.
- [The README didactic pass (spec: run-it split + workstation sweep + vocab box + ten local fixes)](readme-run-it-definitions.md) — SPEC 2026-07-07 (user: the first section defines nothing — what is the N_train learning curve / across all GPUs / bake-off / Optuna; split the one bash block): setup block + five per-driver blocks, each with 1-3 definition sentences (prose VERBATIM in the note: learning-curve falling-vs-flat-tail reading; one-training-per-GPU sentence -> Multi-GPU anchor; the Optuna two-liner; the bake-off head-to-head reading); commands byte-identical, ~12 prose lines, not scary. PLUS the workstation-assumption sweep (user: "you assume everyone will run on my workstation... this code will be for wide use"): the intro generalized to "a machine with a working Cocoa installation — cosmolike supplies the mask and covariance — and, in practice, a CUDA GPU; the package itself is pure PyTorch"; all SIX user-facing "the workstation" sites retired (README intro, code map :70, four driver headers — docstring-only, AST identity; notes/ exempt); gate greps zero. Gate GRI-A. Rides the weight-decay unit (next doc-carrying unit after GAN). NOT implemented.
- [weight_decay must decay only true weight matrices (FINDING, queued behind GAN)](weight-decay-only-weight-matrices.md) — user-raised, Architect-verified 2026-07-07: the `p.ndim >= 2` decay rule misclassifies (1) multigate/gated_power activation params w/beta/mu (K, dim) — decay would kill gates/collapse centers, handicapping the multi-gate families under any nonzero decay — and (2) BinLinear biases (G, out), violating the package's own bias rule. LATENT (all templates ship weight_decay 0.0; no run damaged yet). Fix direction: module-aware allowlist (decay exactly the .weight of nn.Linear/Conv1d/BinLinear; safe failure direction) over parameter tagging. ADDENDUM: the unit also carries the README weight-decay treatment (user directive, didactic: the decoupled-decay equation w <- w - lr*lambda*w + legend, the decayed-vs-never-decayed split diagram, the two-sentence module-role-not-shape why, the paste-ready optimizer.weight_decay YAML block) — docs land WITH the fix, never ahead of behavior; gate: the diagram's decay list byte-matches the implemented allowlist. Discuss + spec AFTER the relu/tanh + norm-knob unit lands (same file, sequential units).
- [relu/tanh families + model.norm knob (IMPLEMENTED + Architect-verified, COMMIT-READY)](activation-families-norm-knob.md) — Implemented 2026-07-07 (Opus, base 83a1e58): make_activation + relu/tanh; FeatureAffine + make_norm (three values, batchnorm rejected without being suggested); build_specs threads model.norm -> block_opts["norm"] beside act (absent = affine, byte-identical — Affine init deterministic, RNG untouched); describe_spec gains norm; bakeoff ACTS stays four + BUILDABLE_ACTS widens --activations; README ### norm subsection (block + feature definition/diagram + table + why-not-batchnorm) + code map + both YAMLs. Architect re-audit ACCEPTED no deltas (own harness 11/11; byte-identity argument verified; all four deviations ruled — the BUILDABLE_ACTS spec gap owned by the Architect). GAN-C workstation open. Next: the didactic pass, then the weight-decay fix. — SPEC 2026-07-07: the paper's "affine normalization gx + b, g b being numbers, between each layer before activation" CONFIRMED = the existing ResBlock default (Linear -> scalar Affine -> act). New: make_activation gains relu/tanh (parameter-free); model.norm whitelist affine (default, byte-identical) | per_feature (new FeatureAffine, dim-sized — the tanh saturation guard) | none; batchnorm DELIBERATELY NOT OFFERED (user deferred to the Architect ruling: batch coupling confounds the bs/EMA experiments, train/eval running-stats split + compiled-twin mode risk, BN buffers sit outside theta_bar, the paper prescribes the affine INSTEAD; per_feature is the escalation; README documents the why in two sentences). Threading mirrors activation (model-level key -> block_opts; describe_spec gains norm; categorical sweep leaf); TRF LayerNorm + CNN heads untouched. Gates GAN-A..C. Sequenced after the TATT micro-commit. NOT implemented.

- [README reorg: two readers, two files (spec, HOLD behind the YAML chapter)](readme-reorg-two-readmes.md) — SPEC 2026-07-07, ON HOLD (user sequencing: "this architecture plan goes after what the implementor is doing right now"): reader-first split — main README = Run it FIRST (simplified; keeps sweep: block + a COMPRESSED Multi-GPU section), then the YAML chapter renumbered 2-11, Pipeline DEMOTED to appendix 12 (stays in main), chi2/activations/precedence 13-15, AI-Usage LAST + cut to the user's verbatim two sentences (screenshot + notebook paragraph deleted, authorized); NEW emulator/README.md code map = Layout, What each file does, Change X -> edit Y, Variants, every-file's-functions LAST (the standing last-rule MOVES there; AI-Usage-last is the main file's twin rule); cross-links both ways; move accounting = nothing lost beyond THREE authorized deletions — the third is the user-approved `git rm -r emulator/parallel/` (dead code, Architect double-checked five legs: zero imports, zero class refs, docstring-only __init__, no MODELS entry ever so no YAML/artifact can dangle, API-stale by its own docstring; five prose mentions enumerated incl. ONE docstring line, AST code-identity still holds). ALSO rides: the GitHub math-rendering fix (user bug report: the section-14 ema equation errors "Double subscripts" — GitHub's markdown strips the \_ escapes before MathJax, so steps_per_epoch = two live subscripts; POLICY: no code-name identifiers with underscores inside $$ math, symbols + legend instead; fixes at sections 14/11/10, single-letter subscripts exempt; gate GRO-G scanner). Gates GRO-A..G. IMPLEMENTED 2026-07-07 (Opus, base fc4655a): both files assembled by a slice-and-relocate script (moved sections byte-identical); `git rm -r emulator/parallel/` staged + all THREE parallel/ docstring mentions cleaned (spec's "five, complete" missed two .py ones — declared); math-render fix at loss/opt/ema (symbols + legends, GRO-G scanner zero `\_`); GRO-A..G all green (11 verbatim moves byte-identical, AST code-identical on both touched .py, py_compile clean). Architect re-audit 2026-07-07: ACCEPTED no deltas, COMMIT-READY — all gates reproduced with own probes (anchors both files + cross-file; moved-content presence; AI-Usage verbatim under normalization — my probe's line-wrap false flag owned; math scanner = single-letter subscripts only; scheduling.py holds the dropped Multi-GPU depth; parallel/ deletion complete). All four deviations RULED ACCEPTED; deviation 1's root cause is the ARCHITECT'S: the "five mentions, complete" grep excluded "in parallel" and swallowed "live in parallel/." at two sites — lesson recorded (exclusion filters can eat true positives; the cycle's second harness bug after the em-dash slugger). Commit sentence in the note. CLOSES the README arc.
- [README YAML chapter (spec, handoff ready)](readme-yaml-chapter.md) — SPEC 2026-07-07 (user: didactic block-by-block YAML reference, "quickly and easily remember the math, the options and what the YAML can do"; each piece a FULL ## section per the user's correction): new sections 7-16 in the user's order (data, training globals, loss w/ the five-mode equations + vote table, optimization stack [grouping veto-able], trim w/ THE shared-schedule plot, focus, ema, model grid w/ mlp/activation/cnn/trf ### subsections, two-phase + phase blocks); section 6 tour shrinks to a pointer; appendices renumber 17-21. STANDING RULE recorded: "Appendix: every file's functions" is ALWAYS the last appendix + owes a completeness update (the four unit-B helpers have no lines). Style binding: LaTeX $$ math verbatim from the tree, one example per section matching example_yamls, dedup by pointers (precedence tables appear ONCE), budget ~350-450 lines. Gates GYC-A..E (math-vs-tree per equation; structure/anchors; dedup; appendix def-list diff; doc-only). Handoff embedded. IMPLEMENTED 2026-07-07 (Opus, on the unit-C base ddb6c98): README sections 7-16 written (all 10 equations verified verbatim against loss_functions.py / training.py / IA), section 6 tour shrunk to a pointer, appendices renumbered 17-21 (every-file's-functions LAST), appendix 21 completed (the four unit-B helpers), all Contents anchors resolve; DOCUMENTATION-ONLY (git diff = README only). GYC-A..E green; the optimizer+lr+scheduler one-section grouping is flagged user-veto-able. Architect re-audit 2026-07-07: ACCEPTED no deltas, COMMIT-READY (chapter read in full; every equation re-verified against the code incl. the vote column re-derived — sqrt = 1/2 flat, berhu plateau sqrt(K/k)/2 = x7.07 at defaults; 61 anchors resolve; dedup + doc-only + scans clean; both deviations ruled ACCEPTED; the grouping stays open for the user at commit). Commit unblocks the readme-reorg HOLD; pending user word on deleting dead emulator/parallel/ within the reorg.

- [Session status 2026-07-07 (read FIRST to resume)](session-status-2026-07-07.md) — compaction-3 snapshot: since -06b, FIVE more units landed and are committed on claude/amazing-keller-e798b6 (2d2f68d doc-audit D-DOC1..9 -> the THREE-UNIT head-activation cycle ebd9869 freeze_trunk / a95f8df per-head activation / ddb6c98 precedence appendix -> fc4655a README YAML chapter), every unit Architect-audited (A accepted; B 30/30; C GPR-A; the YAML chapter GYC-A..E ACCEPTED). Tree clean at fc4655a; only NOTES uncommitted. Mac side fully closed. Remaining: the workstation gate board (now + GFT-C unit A + GHA-F unit B; GM-C still FIRST via checkout 46ec5e1), the production-YAML fixes, then the science. NEXT queued work: [[readme-reorg-two-readmes]] (HOLD lifted by the chapter commit — split into main README + emulator/README.md, delete dead emulator/parallel/, and the GitHub math-render fix for the "Double subscripts" bug the chapter left at sections 10/11/14). Open ruling: the optimizer+lr+scheduler one-section grouping (README §11, user-veto-able). New lessons: sequential-commit discipline, the freeze_trunk role/name split, per-head activation is a construction knob (license + alias), the every-file's-functions-last standing rule, verify-at-build rows can be wrong (ram_frac corrected). Full board + per-unit links in the note.
- [Session status 2026-07-06b (SUPERSEDED by [[session-status-2026-07-07]])](session-status-2026-07-06b.md) — compaction-2 snapshot: Mac side FULLY CLOSED, nine audited feature commits (chain 7ebc061 EMA -> ... -> 8bb5484 ema-anneal+per-phase-ema), tree clean, no open handoffs/deltas. Remaining: merge + WORKSTATION SYNC (broke two runs today; error-message whitelists fingerprint the build), production-YAML fixes (lr block w/ bs_base to top level — phase blocks are DIFFS not containers; nepochs > trunk_epochs before rescnn), then ONE workstation session closes the whole gate board (GM-C FIRST, checkout 46ec5e1 for its pre-EMA golden leg; full ordered list in the note), then the science (berhu-vs-sqrt head w/ softened focus, bs+EMA experiments, final window values). Day's lessons each in their own note: consumed-view STANDING DIRECTIVE, the D-B1 structural-beats-behavioral arc, D-L1 acceptance-over-gracious-error, eager-float exception, berhu naming ruling (exact BerHu in the whitened norm, delta = sqrt(knot), per-sample aggregate). Teaching notebook activation_functions_teaching.nb committed at repo root.
- [model.trf.mlp_width knob (SHELVED by user ruling)](trf-mlp-width-knob.md) — SHELVED 2026-07-06: the user ruled the pin fair ("every MLP layer inside the TRF block is pinned to the token width (26) — this is a fair rule — but make sure it is explicit on documentation"); the knob is NOT to be implemented, do not re-propose unprompted; the explicit-pin documentation duty moved into the doc-audit handoff as D-DOC9. Spec retained intact in the note (ladder dim->w...w->dim, zero-init last, pure mlp_layer_widths helper, gates GTW-A..C) should the ruling ever be revisited; its handoff block is VOID while shelved.
- [train_args.freeze_trunk: joint phase 2 (unit A IMPLEMENTED + Architect-verified, COMMIT-READY)](freeze-trunk-joint-phase2.md) — Implemented 2026-07-06 (Opus, base 2d2f68d): run_emulator freeze_trunk=None kwarg + guard (TypeError non-bool incl. 1/0.0; ValueError explicit + trunk_epochs 0); role/name split (pass role stays "head" for opts/restore, set_train_phase gets "joint") — deviation ACCEPTED; demotion + sweep guards mirror trunk_epochs; SWEEPABLE + banner "(two-phase: N trunk + M joint)"; D-DOC2b obs2+obs3 paid. Architect re-audit ACCEPTED no deltas (own harness 14/14 + AST footprint + statics). GFT-C workstation open. Original SPEC 2026-07-06 (user: "a key allowing trunk and head to trained together"): freeze_trunk bool, default true = today; false = phase 2 trains trunk + head TOGETHER (joint fine-tune warm-started by phase 1). TINY: set_train_phase already accepts "joint" (IA designs 531/845) and the per-pass optimizer covers every param via requires_grad — run_emulator just never calls it; change = phase-2 string "head" if freeze_trunk else "joint" + validation (needs trunk_epochs > 0; demotion drops it like trunk_epochs; sweep-guard single-phase) + banner "(two-phase: N trunk + M joint)". Plus the LICENSE RULE (user directive): a per-head activation (either spelling) requires trunk_epochs > 0 AND freeze_trunk true — teaching error at build_specs (covers freeze_trunk false AND the trunk_epochs-0 extension, veto-able). Name veto-able (alternative joint_finetune: true). Gates GFT-A/B Mac + GFT-C workstation. Lands as COMMIT UNIT A of the head-activation handoff (unit B's license check reads it). UNIT A IMPLEMENTED 2026-07-06 (Opus, on the doc-audit base 2d2f68d): freeze_trunk=None kwarg + guard, the role/name split (set_train_phase "head" if freeze else "joint"), resolve_phase_args + validate_sweep_paths + SWEEPABLE demotion/guard, banners say joint, D-DOC2b obs2/obs3 closed; GFT-A/B green on the Mac, GFT-C on the workstation queue; awaiting commit + Architect re-audit; units B/C follow on the committed base.
- [README precedence appendix (spec, rides head-activation)](readme-precedence-appendix.md) — SPEC 2026-07-06 (user: "we need there an appendix showing many examples of precedence in different flags"): new README appendix "who wins when settings collide" — the activation-slots flagship table (flag > model.activation > H shared; per-head pin holds + warning), phase-block per-key semantics (overlay/full-replace/value/ema-null; bs_base + scheduler cls forbidden), single-phase demotion (trunk > top), loss spellings (both blocks = error, no silent winner), sweep/tune rules, constructor-vs-YAML rows (2 flagged verify-at-build), and the deliberately-NO-knob table (derived eval bs, run-global bs_base, fixed classes, TRF token + MLP width pins). Lands as COMMIT UNIT 2 of the head-activation handoff (the flagship table needs the feature); gate GPR-A. UNIT C IMPLEMENTED 2026-07-06 (Opus, on the unit-B base a95f8df): README section 10 "precedence — who wins when settings collide" (A-G incl. C2), "every file's functions" renumbered to 11, Contents + anchors updated; DOCUMENTATION-ONLY (git diff = README only). Verify-at-build: compile_mode row correct as specced; ram_frac row CORRECTED (only sweep_ntrain/sweep_hyperparam/bakeoff workers force 0; the tune workers divide data.ram_frac by n_workers) — declared. GPR-A green (all 74 anchors resolve, house scans clean, every row cross-checked). Architect re-audit 2026-07-07: ACCEPTED no deltas, COMMIT-READY (doc-only proven; both verify-at-build rows re-verified in the tree — ram_frac correction CONFIRMED with credit; 35 links resolve, my slugger's em-dash false-flags owned; both deviations ruled ACCEPTED). THE THREE-UNIT CYCLE IS CLOSED: A=ebd9869 freeze_trunk, B=a95f8df per-head activation, C=this commit; workstation queue gains GFT-C + GHA-F.
- [Per-head-component activation (unit B IMPLEMENTED + Architect-verified, COMMIT-READY)](head-activation-per-component.md) — Implemented 2026-07-06 (Opus, base ebd9869): the four head_act seams; strict {type,n_gates} translation in build_specs (pure _head_activation_spec + _resolve_head_activation); ruling (a) flag-vs-pin warning (_activation_flag_notice via from_config -> print_design) + bakeoff/sweep _pinned_head_warning; ruling (c) head: alias accepted / trunk: asymmetry-teaching error; ruling (d) license (trunk_epochs > 0 + freeze_trunk not False) at build_specs; D-DOC2b obs1. Architect re-audit ACCEPTED no deltas (own harness 30/30 incl. both-spellings-even-equal + license matrix + notice truth table + phase asymmetry; describe_spec/resolve_phase_args/ACTIVATION_PATHS AST-identical; all 4 seams). Deviations ruled: head_pin helper signature ACCEPTED (alias must warn too); IS->is ACCEPTED (caps rule governs strings). GHA-F workstation open. Unit C (precedence appendix) next on the committed base. — SPEC 2026-07-06: user asked to change activation {type, n_gates} inside the trunk:/head: PHASE blocks. Answer NO twice (eight-key whitelist rejects it; and it could not work — activation is construction-time, the model is built once before phase 1, learnable gamma/beta are trained weights, a mid-run family swap re-inits them under frozen weights; the bs_base phase-blocks-are-diffs ruling again). Delivered instead: the head is identity through phase 1, so head-COMPONENT activation == head-PHASE activation — new optional model.cnn.activation / model.trf.activation (same {type, n_gates} / bare-string schema; absent = share the trunk's, byte-identical; model.activation stays trunk + default). Four seams verified at 8bb5484: head_act=None ctor kwarg falling back to block_opts["act"] (emulator_designs.py:403/712, IA:384/797); build_specs translation (activation joins cnn/trf tables, strict value special-case, NOT mlp); banner FREE via describe_spec (prints the active head dict verbatim — consumed-view satisfied by construction); sweeps as an ORDINARY dotted leaf (ACTIVATION_PATHS exact-match untouched, no driver change). Ruling (a) USER-ACCEPTED 2026-07-06 with amendment: the per-head key pins the head (flag/model.activation govern trunk+default) AND an explicit --activation flag meeting a DIFFERING pin prints a startup warning (pure notice helper; from_config detects — it alone knows the flag was explicit; print_design emits, quiet-gated, the demotion-notice pattern; no warning when flag absent or agreeing); bakeoff + sweep_hyperparam ACTIVATION_PATHS get the same one-line warning when the active head is pinned. Ruling (b) no model.mlp.activation still veto-able; (c) SUPERSEDED by user directive 2026-07-06 — the asymmetry is ACCEPTED as a deliberate special case: head: activation: LEGAL (alias for model.<head>.activation; validate_phase_block accepts it for which=="head" only; consumed by build_specs, never the training resolution; both spellings = error even when equal, recommend canonical; demotion drops it with the notice; sweepable on two-phase), trunk: activation: ERROR with an asymmetry-TEACHING message (the trunk is the same modules in both phases; wording verbatim in the note), README documents the asymmetry (two-phase section + precedence-appendix alias rows). Handoff now carries COMMIT UNIT 2 = the README precedence appendix ([[readme-precedence-appendix]]). Gates GHA-A..E incl. D2 + GPR-A Mac + GHA-F workstation + handoff embedded. Sequencing: doc-audit commit FIRST, then this. UNIT B IMPLEMENTED 2026-07-06 (Opus, on the unit-A base ebd9869): four seams + the {type,n_gates} value special-case, the head: alias + trunk asymmetry error, ruling-(d) license at build_specs, the flag-vs-pin + pinned-head-sweep warnings, D-DOC2b obs1; GHA-A..E green on the Mac (warning helpers take the resolved head_pin so the alias triggers them too; teaching messages verbatim except "IS"->"is" per house caps — both declared), GHA-F on the workstation queue; awaiting commit + Architect re-audit; unit C (README precedence appendix) follows on the committed base.
- [Documentation audit at 8bb5484 (D-DOC1..9, IMPLEMENTED + Architect-verified, COMMIT-READY)](audit-docs-2026-07-06.md) — Implemented 2026-07-06 (Opus): all nine deltas incl. the D-DOC1 banner tuple (loss + ema, additive) and the D-DOC9 token-width pin at seven surfaces. Architect re-audit ACCEPTED, no deltas (own harnesses: AST-minus-docstrings six files CODE-IDENTICAL + one additive tuple hunk; zero stale enumerations tree-wide; README/module-docstring/pin surfaces read directly; scans + py_compile). Rulings: D-DOC9-from-addendum accepted; the Implementer's three surfaced enumerations = D-DOC2b, CONFIRMED rot, assigned to the freeze_trunk/head-activation cycle (units A/B rewrite those regions anyway). ONE commit unit, sentence in the note. — Architect doc audit of README + in-file docs + YAMLs + driver headers against the nine 2026-07-06 commits. CLEAN: zero retired names user-facing; all session-written function docstrings accurate; train_single YAML current but one line. DELTAS: D-DOC1 CODE + consumed-view violation (print_design's sub-block tuple omits "loss" and "ema" — berhu knot/cap/anneal and a top-level ema: never print at startup; fix additive-only, existing banner lines byte-identical so pending gate recipes survive; "loss_mode" label rename DEFERRED post gate board); D-DOC2 five seven-vs-eight enumeration sites (training.py 279/2063/2270, experiment.py __init__, train YAML line 147 w/ paste-ready block in the note); D-DOC3 module-docstring inventories (training.py misses the validator layer + EMA; loss_functions.py misses berhu modes + anneal_value's four consumers); D-DOC4..6 README (EMA absent ENTIRELY; berhu one parenthesis + naming-caveat duty unpaid — exact BerHu in whitened norm, delta=sqrt(knot), chi2-unit knots, per-sample aggregate; appendix inventories missing all validators + derive_eval_bs; no demotion or derived-eval-bs story in section 6); D-DOC7 train_single driver header (no ema key; stale phase list — the other drivers defer to it); D-DOC8 nits (tune-YAML ema search-leaf note, DesignSpec module-docstring mention, ResTRF omission). Gates GDOC-A..E + ARCHITECT_HANDOFF embedded in the note. Fixes NOT yet implemented.
- [ema.anneal: ramp the average's memory](ema-anneal-schedule.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller; clean base = D-P2v2 committed 07e4564): user physics — a full-horizon average started at warmup end still averages in steeply-improving weights all through mid-training (one horizon back keeps ~37%); full smoothing belongs where the trajectory is flat. Mechanism: schedule the horizon, not a blend — h(e) = horizon_epochs*s(e) (s = the trim-style 0->1 schedule {hold_epochs, anneal_epochs, shape}, the berhu-anneal twins), beta(e) = derive_ema_beta(h(e), steps) — the existing denom<1 clamp gives beta=0 through the hold (theta_bar tracks theta, zero memory of the bad era), continuous everywhere (clamp boundary beta(1/steps)=0 rising smoothly), no-anneal = the s==1 special case exactly. training.py: _validate_berhu_anneal generalized to one shared _validate_anneal_block(anneal, which) (q=train_args.{which}.anneal; berhu passes f"{which}.berhu", both paths regression-gated); _EMA_KEYS += anneal; validate_ema gains which= (paths train_args.ema / trunk.ema) + returns {horizon_epochs, anneal}; training_loop_batched builds ema_s_opts (start 0/end 1) + recomputes beta per epoch beside the anneal_value calls — beta stays a per-epoch Python float, the EMA lerp is EAGER (outside all compiled graphs; a mandated comment forbids tensor-izing it or copying the float pattern to compiled schedules); live point = max(warmup end, first s>0); no-anneal keeps the constant target beta + byte-identical banner, annealed banner marks target with "beta -> ..." + schedule. GMP (user-found phase gap): "ema" rejoins _PHASE_BLOCK_KEYS (8, full replacement); validate_phase_block validates the phase ema (ema: null = the per-phase opt-out of an inherited block, validate_ema(None)->off); run_emulator resolves ema_pass beside loss_pass (key-present incl null replaces, absent inherits; validate with trunk.ema/head.ema), passes ema=ema_pass, phase banner notes it; demotion (prefix-strip) + sweep (generic strip, SWEEPABLE already carries ema) automatic — trunk-only/head-only/different-horizon ema all legitimate (theta_bar re-inits per pass). Docs: experiment.__init__ + run_emulator docstrings, train_single YAML anneal twins + ema: null note, weight-ema note pointer. Gates GME-A (both-helpers math: beta 0 through hold + while h*steps<1, ramp-end == no-anneal beta exactly, clamp-boundary continuity, cosine/linear, hold-0 + anneal>>nepochs edges), GME-A2 (_EMA_KEYS, 9 shared-validator rejection cases on BOTH paths path-correct, validate_ema None/off), GMP-A (phase resolution + null opt-out + demotion + trunk.ema/head.ema error paths), GME-B/GMP-B (static: _PHASE_BLOCK_KEYS(8), ema_pass beside loss_pass, ema=ema_pass, shared-validator rename, live point, per-epoch beta, eager-float comment, byte-identity, banner) ALL PASS on the Mac (exec-extract + numpy, no torch). GME-C workstation (golden no-anneal byte-identity + {hold 5, anneal 10} live-point smoke) embedded. AWAITING: the user's commit (sentence in the note) + Architect re-audit. Declared: validate_ema gained which (default train_args, back-compatible) + returns {horizon_epochs, anneal} (was the input dict). IMPLEMENTED + Architect-verified 2026-07-06 (28 checks: beta schedule exact incl. ramp-end == target and clamp continuity; both validator paths name their owners; trunk-only/head-only/null-opt-out/demotion legs exact; whitelist 8; eager-float comment; guard-gated byte-identity). Deviations accepted (validate_ema which= + normalized return). Commit sentence in the note; GME-C workstation only.
- [STANDING DIRECTIVE: banners print the consumed view](banner-prints-consumed-view.md) — user directive 2026-07-06 after TWO same-day display bugs (single-phase resmlp printing '(two-phase: 2000 trunk + -1000 head)' from raw trunk_epochs; model spec dumping raw cnn:/trf: blocks TemplateMLP ignores). RULE: every display surface renders the RESOLVED/CONSUMED config via the SAME functions the execution uses (resolve_phase_args, head selection, validate_loss) — tolerant consumption, truthful display; every future config feature's spec MUST state its banner rendering (joins verbatim numerics + gates as mandatory blueprint sections); every audit MUST check display surfaces (GP-B checked train() and missed print_design — the gap that shipped D-P2). IMPLEMENTATION IDIOM (user's tip): the class prints itself — head_block class attr + shared describe_spec(model_block) classmethod on every design; print_design delegates; ARCH_HEAD retired in favor of model_cls.head_block (one source for consumption + geometry + display). Concrete fixes = the D-P2v2 delta in resolve-phase-args-single-phase.md. D-P2v2 IMPLEMENTED + Architect-verified 2026-07-06 (18/18: resolved-view banner both legs; DesignSpec mixin with __init_subclass__ fail-at-import; describe_spec per class; ARCH_HEAD zero refs; build_specs one-line diff = behavior identical). Commit = the D-L1v3 + D-P2v2 unit (sentence in resolve-phase-args note).
- [loss.berhu.anneal: sqrt -> berhu schedule](berhu-anneal-schedule.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller; clean base = loss-block-nesting committed dcbaf9f): user physics — early training floods the (knot, cap) window with big outliers passing DOWN through it (chi2 > cap), so escalated window votes belong in LATE training. Optional anneal: sub-block inside loss.berhu with trim's argument names {hold_epochs, anneal_epochs, shape}: L_s = (1-s) sqrt(c) + s L_berhu(c); s = 0 through hold, then 0 -> 1 via the EXISTING anneal_value helper (start 0, end 1, no new schedule code); every intermediate C1 free (convex combo of C1 endpoints); monotone in s beyond the knot. Activation = block PRESENCE (ema/focus pattern; Architect interpretation of the user's bool, veto-able). training.py: validate_berhu whitelist -> {knot, cap, anneal} and ALWAYS returns {knot, cap, anneal} (anneal validated-or-None); new pure _validate_berhu_anneal + _ANNEAL_KEYS {hold_epochs, anneal_epochs, shape} + _ANNEAL_SHAPES (const/linear/cosine/step): all 3 keys required, hold>=0 int, anneal_epochs>=1 int (bool rejected), shape whitelisted. training_loop_batched builds s_t (None when absent, else 0-dim device tensor) + s_opts {start 0, end 1, **anneal} beside knot_t/cap_t; fills s_t in place per epoch via anneal_value beside trim_t/focus_t; _fwd_loss gained a berhu_s param passed in BOTH loss() calls, threaded from the call site (trim_t/focus_t arg pattern); banner appends "; anneal: hold H + A shape". loss_functions.py: berhu_s=None on loss()/_reduce (forwarded); each berhu branch keeps today's where() form VERBATIM then adds, guarded by `if berhu_s is not None`, v = (1-berhu_s)*sqrt(c) + berhu_s*v — blend ops enter the graph ONLY when berhu_s passed (static per-pass specialization -> no-anneal run byte-identical). RescaledChi2/IA forward via *args/**kwargs (berhu_s free); ElementWeightedChi2 untouched (no berhu). experiment.__init__ loss doc; train_single YAML commented anneal sub-block; tune anneal search-leaf comment. Doc-coherence FLAGGED beyond target list: IA/loss_functions.py 2 forwarding docstrings enumerate berhu_s. Edge cases confirmed legal+documented: hold 0 (ramp from epoch 1), anneal>>nepochs (s never reaches 1). Gates GBA-A (blend + schedule math: 3 knot pairs s=0->sqrt/s=1->mode exact, s=0.3 convex combo, monotone in s, C1 straddles both knots; cosine/linear hold-boundary exact + monotone), GBA-A2 (13 validator cases), GBA-B (14 static: blend twice each guarded, both where() forms textually today's, s_t in-place from anneal_value no new schedule code, both _fwd_loss + 5-arg call site, whitelist+constants, banner) ALL PASS on the Mac (numpy + exec-extract + static, no torch). GBA-C workstation (golden no-anneal byte-identity + {hold 5, anneal 10, cosine} hold-boundary-continuity smoke) embedded in the note. AWAITING: the user's commit (sentence in the note) + Architect re-audit.
- [train_args.loss nested block](loss-block-nesting.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller; clean base = berhu committed 64786a7 with D-B1 folded in): user directive — loss options localized in one block: loss: {mode: sqrt|chi2|sqrt_dchi2|berhu|berhu_capped, berhu: {knot, cap}} replaces flat loss_mode + top-level berhu (absent = {mode: sqrt}, byte-identical to today). PAYOFFS realized: D-B1 inheritance machinery DELETED (run-level inertness guard + phase_owns_berhu split both gone — knots travel with their mode, so "berhu sub-block on a non-berhu mode" is a purely LOCAL check inside validate_loss); EARLY mode-string whitelist (_LOSS_MODES) at config time (a typo'd mode used to survive to the first compiled loss call); phase whitelist 8 -> 7 ("loss" full-replacement replaces "loss_mode"+"berhu"). training.py: pure validate_loss(block, which) beside validate_ema (None->{mode:sqrt,berhu:None}; unknown key/mode loud naming the five; routes loss["berhu"] through validate_berhu with which_berhu="loss"/"{phase}.loss" so paths render train_args.loss.berhu; returns berhu={knot,cap} for a berhu mode else None); validate_berhu's dead loss_mode=None hook removed (now unconditional `if not is_berhu`); _loss_migration_message builds the paste-ready nested block; validate_phase_block rejects flat loss_mode/berhu with that message + validates the loss sub-block; _PHASE_BLOCK_KEYS = 7 (loss replaces loss_mode+berhu); run_emulator signature loss= replaces loss_mode=/berhu=, up-front validate_loss + per-pass full-replace+validate_loss (mode_pass/berhu_pass from it); knot_t/cap_t threading + both _fwd_loss calls + banner ("loss_mode {mode}" text unchanged) UNTOUCHED; loss()/_reduce internals UNCHANGED. experiment.py: train() passes loss=train_args.get("loss") (drop loss_mode=/berhu=); resolve_phase_args rejects flat top-level loss_mode/berhu before the early return (covers two-phase too, since experiment reads only train_args["loss"]); banner reads (loss or {}).get("mode"); __init__ docstring loss block. sweep driver SWEEPABLE_TOP_KEYS: -loss_mode -berhu +loss. Docs: 3 example YAMLs (loss: block, commented berhu sub, trunk/head examples), tune loss.mode comment, README, + doc-coherence beyond the target list (FLAGGED): loss_functions.py stale train_args.berhu.knot/.cap -> train_args.loss.berhu.* (2 docstrings + 2 runtime guard messages, code branches unchanged) and train_single driver header loss_mode -> loss. Gates GL-A (validate_loss: 14 cases incl. defaults, berhu-mode knots default, LOCAL berhu-on-non-berhu raise, unknown mode/key, knot>=cap, phase which path), GL-B (migration on BOTH paths, values carried, single-phase-demotion error == direct validate_phase_block error), GL-C (static: run_emulator loss= and NO loss_mode=/berhu=, phase_owns_berhu + inertness-guard GONE, whitelist 7, SWEEPABLE swap, knot_t/cap_t both calls, banner, None-hook removed) ALL PASS on the Mac (exec-extract, no torch). GL-D workstation (golden equivalence new-schema vs pre-change; berhu GB-C leg-2 recipe updated to head-owns-loss). AWAITING: the user's commit (sentence in the note) + Architect re-audit. Supersedes the berhu schema (train_args.berhu {knot,cap} -> train_args.loss.berhu).
- [loss_mode berhu + berhu_capped](loss-mode-berhu.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller; clean base, phase blocks 76ef641; the note was revised twice mid-build, derived-knot -> derived-two-knot -> Revision 2 YAML knots, delivered Revision 2): user-derived from the gradient-weight analysis (sqrt = every sample an equal vote |r|^0; p<1/2 chases the noise floor; chi2 starves the bulk), GENERALIZED by the user to a capped variant, then (Revision 2, user directive) both knots made YAML parameters. One three-regime family, two mode strings: "berhu" = sqrt(c) below knot, (c+knot)/(2 sqrt knot) above; "berhu_capped" additionally returns to sqrt-shaped above cap — region 3 = a*sqrt(c)+b with a=sqrt(cap/knot), b=(knot-cap)/(2 sqrt knot) — C1 at BOTH knots by construction. Votes: 0.5 bulk, rising x7 across 0.2..10, plateau for monsters (uncapped berhu: unbounded ~11 at c=100). YAML: optional train_args.berhu {knot: 0.2, cap: 10} (defaults = _BERHU_DEFAULTS; pure validate_berhu: block+non-berhu-mode raises like trunk-without-trunk_epochs, knot<cap, cap accepted-unused under plain berhu for sweep-friendliness); "berhu" joins _PHASE_BLOCK_KEYS (8, full replacement — resolve_phase_args prefix-strip covers it with NO change) and SWEEPABLE_TOP_KEYS. ONE change point: the shared selfless _reduce ladder (IA losses forward kwargs); knot_t + cap_t 0-dim device tensors built once per pass beside kappa_t (closure floats kill CUDA-graph replay); eval/metrics/EMA untouched (chi2 path); per-phase via head: loss_mode; banner prints the knots. Interplay recorded: berhu carries focus's tail role (soften head focus for clean attribution); berhu_capped may make clip/late-trim redundant (measure). Gates GB-A (numpy continuity at both knots, 3 knot pairs, exact reductions) + GB-A2 (validate_berhu: defaults, block-on-non-berhu-mode raise, knot<cap, bool/str/<=0, cap-under-berhu accepted) + GB-B (static: one ladder, both where() verbatim, _BERHU_DEFAULTS, knot_t/cap_t from the resolved block in both _fwd_loss calls, per-pass resolve+validate, berhu in _PHASE_BLOCK_KEYS=8 + SWEEPABLE, IA docstring-only, doc sweep, scans+compile) all PASS; GB-C workstation (unbound _reduce torch script incl. berhu_capped==berhu below cap + non-default knots + autograd straddles at both knots; golden byte-identity; real berhu_capped head run vs sqrt baseline) written+embedded in the note. Architect re-audit 2026-07-06: ACCEPTED with ONE delta D-B1 (a spec gap the Architect owns) — the per-pass wiring mode-checks the INHERITED top-level berhu block against every pass's mode, so the legitimate mixed config (top-level block + berhu only on the head + sqrt trunk) raises at the trunk pass; fix = pass loss_mode=None for inherited blocks (the validate_berhu hook exists), keep strict for phase-scoped blocks, add a run-level inertness check up front (no berhu mode anywhere -> raise); GB-C real-run leg switches to the mixed config. All other gates reproduced (own harness: 3-knot-pair continuity + exact reductions; 15 validator cases; whitelist 8 + SWEEPABLE + double threading; IA file proven code-identical by docstring-stripped AST; CPU/CUDA scan flags were my harness's too-narrow allowlist, not violations). D-B1 CLOSED 2026-07-06 (Opus) + Architect-verified same day (own harness, six scenarios: mixed passes end-to-end incl. reverse mix; inert raises both shapes; phase-owned strict; inherited still value-validated; guard before setup) — feature fully ACCEPTED, GB-C workstation only (real-run leg = the mixed config). Commit sentence in the note. Motivation: frac>0.2 at 0.032, median 0.017.
- [Phase blocks: nested lr + per-phase scheduler](phase-blocks-nested-lr-scheduler.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller; clean base, EMA committed 7ebc061): trunk:/head: now mirror the top-level train_args schema key-for-key. Seven-key phase whitelist {lr, scheduler, loss_mode, trim, focus, clip, rewind}: lr = overlay {lr_base, warmup_epochs} (bs_base rejected, run-global), scheduler = kwargs full-replacement (cls rejected). Pure validate_phase_block(block, which) + _PHASE_BLOCK_KEYS in training.py (imported by experiment.py — no reverse import; training does not import experiment), called on BOTH paths (run_emulator before setup, resolve_phase_args before demotion, incl. head: even though it is dropped, so a shared YAML fails loudly either way); flat lr_base → loud paste-ready nested-lr: migration; non-mapping/bs_base/cls/unknown all raise (closes the resolve-phase-args looseness). run_emulator consumption: lr_pass from phase lr.lr_base (sqrt rule), wmupe_pass from phase lr.warmup_epochs threaded per pass, sched_pass={cls: run's, **phase} to make_scheduler, banner tail += warmup/scheduler. resolve_phase_args demotion = prefix-strip (lr overlays preserving bs_base, rest full-replace; lr_base special case deleted); validate_sweep_paths concretization = generic ".".join strip (trunk.lr.lr_base→lr.lr_base, trunk.scheduler.patience→scheduler.patience). Docs: 3 YAMLs (train_single nested trunk:/head: + head scheduler patience-10 + warmup_epochs examples; sweep + tune head.lr.lr_base), both driver headers, set_by_path docstring, README x2. Gates GH-A (validator) + GH-B (overlay preserves bs_base, scheduler full-replace, deep-unmutated, generic strip) + GH-C (static wiring both paths) + GH-D (scans + grep: head.lr_base nowhere in py/yaml/README + compile) all PASS; GH-E (patience-10 head cadence + no-blocks golden run) workstation recipe in the note. Declared: constant renamed _PHASE_BLOCK_KEYS (single whitelist in training.py); head: validated on the demotion path too. Architect re-audit 2026-07-06: ACCEPTED, NO deltas (own harness 36/36: validator cases incl. value-carrying migration message; demotion lr-overlay preserves bs_base AND merges phase warmup; scheduler full-replace; malformed-head raises despite drop; generic-strip sweep concretization; wiring line-ordered 1625/1626 < ema 1630 < model 1686; stale-schema grep empty tree-wide; YAML block matches paste). Both deviations accepted — (2) formally supersedes resolve-phase-args' "head dropped unvalidated" and CLOSES its noted looseness in full. ONE clean commit unit (sentence in the note); GH-E workstation only.
- [Weight EMA coupled to snapshot/rewind](weight-ema-snapshot-coupled.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller): Polyak weight averaging as the cheap fix for the batch-size generalization gap (frac>0.2: 0.065 @ bs64 vs 0.10 @ bs768 despite sqrt-lr). INVARIANT (held): anything the rewind un-lives must vanish from the average — the best snapshot is ONE unit {θ, optimizer, θ̄}, saved on new best, restored on lr cut. training.py: pure validate_ema (whitelist {horizon_epochs}, off=None sentinel) + pure derive_ema_beta = 1−1/(h·steps_per_epoch) (bs-invariant, clamp 0). training_loop_batched threads `ema` with ALL branches guard-gated (ema_on/theta_bar/best_is_ema) so absent=byte-identical: θ̄ init at warmup end, foreach_lerp_ update after optimizer.step, per-epoch raw eval (drives scheduler) + in-place foreach copy_ swap for the average eval (drives selection+printed), snapshot carries θ̄, rewind restores θ̄ (best or re-based when best predates ema via a best_is_ema flag), post-loop ships best θ̄; never a .data rebind (CUDA-graph storage). run_emulator gained `ema` (validates up front, passes ema=ema per phase → re-init at handoff); experiment.train passes train_args.get("ema"); "ema" joins SWEEPABLE_TOP_KEYS; commented ema block in train_single YAML. Gates GM-A (β/config pure) + GM-B (static: 9 ema calls all guard-gated, lerp after step, snapshot carries θ̄, no new .data=, wiring+scans+compile) all PASS; GM-C (off-mode bit-identical golden run) + GM-D (on-mode smoke: banner, post-rewind EMA jumps WITH raw, shipped=best avg) workstation recipe in the note. Declared interface: run_emulator + training_loop_batched gained `ema`. Architect re-audit 2026-07-06: ACCEPTED with ONE naming micro-delta D-M1 (`_ema_KEYS` -> `_EMA_KEYS`; caps rule targets emphasis not acronyms — EMA joins the scan allowlist beside RAM/YAML/GPU, Architect ruling) — all GM-A/B claims reproduced (own harness: 14 pure cases incl. bs-invariant retention 0.7164 vs 0.7153; line-ordered swap/snapshot/rewind/ship wiring; scheduler on raw median only; 3 eval sites on eval_bs; zero code .data rebinds); best_is_ema fallback accepted within the invariant; D-E1/D-E2 verified closed; GM-C/D recipe sound. D-M1 CLOSED 2026-07-06 (Opus): _ema_KEYS -> _EMA_KEYS at all 3 sites (the GM-B de-cap replace_all had mangled the constant); caps scan with EMA allowlisted = CLEAN, py_compile OK. Commit = ONE unit (folds D-E1/D-E2: 46ec5e1 shipped eval-bs without them); then the phase-blocks feature can start on a clean base. Related: bs-comparison confound (schedules epoch-denominated) noted, not fixed.
- [Eval batch derived from n_val (no knob)](eval-bs-decoupling.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller): eval_val used to run at the TRAINING bs, so small-batch runs doubled eval launches too (bs=32: ~157 eval batches ≈ 0.3 s of the 2.4 s/epoch launch-bound regime). No knob (revised design): pure derive_eval_bs(n_val, target, load) in training.py — k = ceil(n_val/target), bs = ceil(n_val/k), clamp to the stream chunk `load`, n_val<1 raises; pad = k*bs-n_val < k always (n_val=5000 -> bs=1000, ZERO pad, beats 1024's 2.3% and 2048's 18.6%); _EVAL_BS_TARGET=1024 module constant; enabled by absolute n_val exact at config time. training_loop_batched computes eval_bs ONCE before the baseline eval; both eval_val sites pass bs=eval_bs; eval_val bs docstring drops "same as training". training.py-ONLY + one train_single YAML comment — run_emulator/training_loop_batched/experiment.train signatures untouched, no YAML key, experiment.py + SWEEPABLE_TOP_KEYS untouched. Gates GE-A (pure: verbatim table + n_val=1 + load clamp + n_val<1 raise + 1e4-pt property sweep pad<k, worst 0.098%), GE-B (static: one derive before baseline, both sites bs=eval_bs no bs-leak, no eval_bs key, signatures intact, scans/width/comprehension/compile) all PASS; GE-C (torch-only partition-invariance rtol 1e-6 across bs {32,517,1000,2048} + eval-timing + real bs=32 epoch-drop) is written + embedded in the note, rides the workstation queue. Architect re-audit 2026-07-06: code ACCEPTED (own harness: table + edges + clamps + raises + 2e4-pt sweep worst 0.09765%; wiring exact; no eval_bs anywhere) with TWO micro-deltas — D-E1: the embedded GE-C script builds thresholds on DEV but eval_val compares them against the CPU-moved c, so on CUDA the first call crashes (drop device=DEV from thresholds only); D-E2: the two YAML continuation-comment lines sit one hash-column short of the file's (align to col 26). D-E1/D-E2 CLOSED 2026-07-06 (Opus): note's GE-C thresholds moved to CPU with a comment; the two bs continuation comments realigned to col 26 (verified). eval-bs is commit-ready. Clean base (sweep-guards landed as 4471539), BUT the weight-ema feature now also touches training.py + train_single YAML + MEMORY on top, uncommitted → if eval-bs is not committed first the two share those files (git add -p or one combined commit).
- [Driver audit: phase-sweep guards + doc parity](driver-audit-phase-sweep-guards.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller): full audit of sweep_hyperparam / sweep_ntrain / tune / bakeoff + YAMLs against the 4 recent features. CLEAN: zero stale keys anywhere; all drivers train via exp.train() (demotion uniform); bakeoff/sweep_ntrain fully transparent; set_by_path deep-copies. REGRESSION found: resolve_phase_args removed the loud stop for sweeping/searching trunk_epochs / head.* on a single-phase model — N identical points (or dead Optuna dimensions in tune, whose shipped YAML advertised two-phase keys beside name: resmlp); trunk.* sweeps were functional-but-disguised. Fix: pure validate_sweep_paths(paths, two_phase) in experiment.py (head/trunk_epochs -> reject "axis dropped by resolve_phase_args"; trunk.X -> reject naming the concrete top-level key, lr_base->lr.lr_base; mixed list -> one error listing all), called at startup by sweep_hyperparam (the swept param, skipped for act_mode) after from_config before both dispatch paths, and by tune (list(ranges) = the search-range dotted paths, search_defaults returns {path:default}) before any worker spawns; both probe hasattr(exp.model_cls,"set_train_phase"). Stale comments fixed (set_by_path docstring + sweep YAML x2 + tune YAML) with the D-P1 wording. Gates GD-A (pure: head/trunk_epochs/trunk messages, mixed-list, valid paths pass, two_phase silent), GD-B (static call-order both drivers), GD-C (scans + width + py_compile + D-P1 grep) all PASS; GD-D none owed (early config error). Architect re-audit 2026-07-06: ACCEPTED (own harness: rejection wording + trunk concretization incl. lr.lr_base, one-error mixed list, exact-segment match, wiring line-ordered in both drivers, D-P1 phrases gone tree-wide, code/YAML caps-clean). Lands as ONE commit with D-P1 (2b7a2af shipped without it; shared experiment.py hunks) — commit sentence in the note. Nothing open; workstation queue unchanged.
- [resolve_phase_args: two-phase YAML on single-phase models](resolve-phase-args-single-phase.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller): one shared YAML across architectures. trunk_epochs/trunk:/head: on a model without set_train_phase used to die in run_emulator (user hit it: name: resmlp + two-phase YAML -> "this model is OptimizedModule"). Pure resolve_phase_args(train_args, two_phase) in experiment.py (beside validate_sizes; + _PHASE_TRUNK_KEYS), called once at the top of train() (the choke point for run/tune/sweeps/bakeoff) probing hasattr(self.model_cls,"set_train_phase") on the CLASS — single-phase: shallow-copy, drop head:+trunk_epochs+trunk, merge each trunk: key into the top level (six-key table; lr_base nests into lr.lr_base preserving bs_base/warmup, other five full-replace), quiet-gated notice naming only what merged/dropped, never mutates input, unknown trunk key raises naming the six; two-phase: exact no-op (same object). training.py capability error now unwraps _orig_mod (names the real class); both run_emulator guards byte-identical. Gates GP-A (pure, incl. user's exact block: top-level trim/focus survive, notice names loss_mode only; + all-six, unknown, head-only, deep-unmutated), GP-B (static wiring: call-order, probe, self.log, _orig_mod, guards unchanged), GP-C (scans + width + no-new-comprehension + py_compile) all PASS. GP-D (failing YAML trains + rescnn+nla no-op control) rides the workstation queue. Architect re-audit 2026-07-06: ACCEPTED with ONE doc-only delta D-P1 — the added examples "e.g. resmlp / nla" (experiment.py x2) and "an ia without a factored head" (YAML) mislead: nla is not a single-phase marker (rescnn+nla IS two-phase) and factored != two-phase (TemplateMLP is factored AND single-phase); reword to "any model class without set_train_phase (every resmlp incl. ia: nla/tatt)". D-P1 CLOSED 2026-07-06 (Opus): all three doc spots (experiment.py __init__ docstring + train() comment + train_single YAML comment) reworded to "any name: resmlp incl. ia nla/tatt, no set_train_phase; two-phase = rescnn/restrf"; scans + py_compile + tree-wide grep clean. Noted, not delta: scalar trunk: value silently dropped ("no keys" notice) — same looseness as the unvalidated two-phase blocks, one future item makes both loud. Its own commit unit (2nd of the two, after D-P1 which folds in here).
- [n_train / n_val absolute counts (post-cut)](n-train-n-val-absolute-counts.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller; on a clean base, features 3+4 committed as dba7588): replaced data.train_divisor / val_divisor with REQUIRED absolute data.n_train / n_val, enforced AFTER param_cuts (the divisor path computed keep = N_raw // divisor on the PRE-cut dump — the one pre-cut leak; load_source already takes rows from the post-cut pool + raises pool-too-small). Pure validate_sizes (bool rejected; missing/<1/non-int loud) + no-alias divisor migration message (semantics changed, no auto-conversion, placeholder values); load_source drops divisor / n_keep now a required arg; banner sizes line + `used K of P cut rows`; sweep n_est = exact n_train + meta key rename; README/YAMLs/train_single header swept. Gates GS-A (pure validation + migration message verbatim), GS-B (real module exec'd with torch+psutil stubs: exact count, cut-satisfaction, pool-too-small message, prefix nesting), GS-C (diff-based scans + width + no-new-comprehension + keyword-vs-signature + py_compile) all PASS. GS-D (banner sizes + used-K-of-P on a real run) rides the pending workstation session. Architect re-audit 2026-07-06: ACCEPTED with ONE delta D-1 — load_source lacked a keep<1 guard, so an explicit stage_train(n_train=-5) silently staged phys[:-5] (pool minus 5; probe-confirmed). D-1 CLOSED 2026-07-06 (Opus): loud ValueError after keep=int(n_keep) ("n_keep must be >= 1 ..., got {keep}"), GS-B extended with the -5/0 cases (both raise), GS-B still ALL PASS. Architect verified D-1 (probes -5/0 raise; regressions intact) — feature ACCEPTED in full, only GS-D open. Commit correction: the feature was committed AND merged to main as 906528c BEFORE D-1, so D-1 is its OWN follow-up commit (data_staging.py + the note), ready now.
- [Session status 2026-07-06 (compaction-1, SUPERSEDED by -b)](session-status-2026-07-06.md) — the first-compaction snapshot; superseded by [session-status-2026-07-06b](session-status-2026-07-06b.md) (the current read-first). compaction-1 snapshot: 2 features committed (audit fixes, window cuts), 2 audit-ACCEPTED awaiting the user's split commit (param_cuts nesting, triangle shading; block in the note), consolidated workstation queue (ONE train_single run closes G-F+GN-F+G1-import+GT-C; item-27 A/B separate), t16 run = coverage-limited, session rules (raw outputs in handoffs, declare interface deviations, commit-sentence + merge block, YAML paste-ready rule).
- [Session resume 2026-07-06 (SUPERSEDED redirect)](session-resume-2026-07-06.md) — the Implementer's FIRST-compaction pointer, now stale (it says tip 76ef641 with berhu still uncommitted). Both false: the berhu arc + loss-nesting + berhu/EMA anneal + D-L1v3 + D-P2v2 all committed and Architect-verified, tip 8bb5484, tree clean. Read [session-status-2026-07-06b](session-status-2026-07-06b.md) instead (the current READ-FIRST). Kept only as a redirect stub.
- [Triangle cut shading: all windows](triangle-cut-shading-all-windows.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller): plotting.py only. _cut_exclusion -> _window_masks returns per-window (name, mask) for all 4 windows (omegabh2, omegam2h2 3-ways, omegamh2 on (om,h0) + a 1-D band on any omh2-axis panel, omegamh2ns on (ns,omh2)); _CUT_ROLES += ns; _shade_cuts superposes one _CUT_GREY=(0.55,0.55,0.55,0.30) contourf per window (alpha-stacks, zorder 0) + _shade_omh2_marginal axvspans the omh2 1-D marginal; formulas carry provenance to phys_cut_idx's _omega_* table. Gates GT-A (mask==reference on every sharp panel + exact ~keep cross-check vs phys_cut_idx at 20k sample coords, sha/count-identical) + GT-D (scans+compile+grey-only palette) PASS; GT-B (render) + GT-C (visual PDF) workstation-deferred (no matplotlib/getdist on the Mac). Resume + evidence in the note.
- [data.param_cuts nested block](param-cuts-nested-block.md) — IMPLEMENTED 2026-07-06 (Opus, amazing-keller): the 8 cut keys moved from flat data: into a nested data.param_cuts: sub-block, legacy omegabh2_cut RENAMED omegabh2_hi (incl. the internal cut kwarg of phys_cut_idx/load_source). from_config calls a standalone pure validate_param_cuts (DATA_KEYS shrunk, new PARAM_CUTS_KEYS whitelist); stage/pool read pc=d["param_cuts"]. Flat keys -> ValueError migration error printing the paste-ready block (ValueError not KeyError: KeyError reprs+escapes the newlines). Beyond the target list but forced by the hard rename: plotting.py (_shade_cuts key) + train_single (its diagnostics cuts dict) followed the rename (else a silent shading break). cocoa.py verified transparent (fixed _DATA_PATH_KEYS tuple). Gates GN-A (byte-identical nested==flat, sha256), GN-B (4 loud errors), GN-C (3 YAMLs pass), GN-E (scans+keyword-sig+compile) all PASS; GN-F workstation smoke deferred. No aliases, hard break. Resume + evidence in the note.
- [omegamh2 + omegamh2*ns window cuts](omegamh2-ns-product-cuts.md) — IMPLEMENTED 2026-07-05 (Opus, amazing-keller): optional windows omegamh2_lo/hi (pure Om (H0/100)^2, Planck ~0.143) + omegamh2ns_lo/hi (times ns, ~0.138) extending phys_cut_idx, now a QUANTITY TABLE (4 formula helpers + a windows list + one loop, adding a window = 1 row); returns (kept_idx, report) for per-window survivor counts in the load banner; DATA_KEYS whitelist in from_config catches the omegamh2-vs-omegam2h2 one-char typo; lo>=hi + missing-ns/unknown-key raise loudly. Gates G-A (exact numpy mask, sha256), G-B (back-compat byte-identical), G-C (3 loud errors), G-D (banner), G-E (house scans + compile) all PASS; G-F workstation smoke deferred (no torch/cosmolike on the Mac). Both windows default off = byte-identical no-op until the user sets values. Resume + raw evidence in the note.
- [AI collaboration preferences (portable)](ai-collaboration-preferences.md) — read-me-first mirror of the ~/.claude auto-memory (which is path-keyed and won't survive the repo move); indexes code style / shape-flow / dual-agent / plots / machines, and captures IN FULL the two that lived only in auto-memory: YAML block style + the repo-notes ritual.
- [Dual Fable/Opus workflow](dual-fable-opus-workflow.md) — .claude/FABLE_ROLE.md (Fable 5 architect/auditor) + OPUS_ROLE.md (Opus 4.8 implementer); handoff blocks persist to notes/ first; per-domain gates (CAMB ratio/!VM, cosmolike chi2, emulator frac>0.2); raw-output reporting; AUDIT = Fable-ONLY (user directive — never Opus, never self-check, cost is no excuse).
- [Weight decay only on weight matrices](weight-decay-only-on-weight-matrices.md) — don't decay learned-activation/norm-gain/bias params; wd=0 or ndim>=2 split.
- [Hanging indent, not paren alignment](hanging-indent-not-paren-alignment.md) — wrap multiline calls/signatures with a 2-space hanging indent, never align under the opening paren.
- [Locate notebook edits by context](locate-notebook-edits-by-context.md) — point to notebook spots by function/marker/quoted line, never cell or line numbers (user can't see those).
- [Dev machine: Mac M2, 32 GB, MPS](dev-machine-mac-m2-32gb.md) — test on M2/MPS (no device float64, unified 32 GB pool), train on NVIDIA; code branches on device.type.
- [Docstrings: formal Arguments block](docstrings-formal-arguments-block.md) — every docstring lists ALL params in a formal Arguments: block (+ Returns), never prose covering only some.
- [Plots: no red+green](plots-no-red-green.md) — colorblind accessibility; explicit colorblind-safe palette per line, never the default cycle.
- [Emulator pipeline and goal](emulator-pipeline-and-goal.md) — pytorch1.ipynb cosmic-shear emulator; goal 90% under delta-chi2<0.2; select by frac>0.2; component map.
- [Geometry/loss composition](geometry-loss-composition.md) — POST-REFACTOR architecture: CosmolikeChi2 HOLDS a DataVectorGeometry (self.geom), not inherits; build geom once, wrap in CosmolikeChi2/RescaledChi2(A)/ResidualBaseChi2(B)/ElementWeightedChi2(geom); pipeline branches on getattr(chi2fn,"needs_params",False) NOT isinstance; build_shear_angle_map(geom); diagnostics must branch decode/encode/chi2 on needs_params + pass whitened params. A vs B = where R touches d_pred (A divides net output -> diag(1/R) conditioning; B moves only the baseline, plain chi2); order-unity, B is the clean isolation experiment.
- [Construction via spec dicts](construction-via-spec-dicts.md) — model/opt/scheduler built from {cls, **kwargs} dicts + make_X helpers; injected args stay out.
- [Probe-generalization bugs](probe-generalization-bugs.md) — always hunt xi-only assumptions that break ggl/wtheta; block-local vs global index, full-3x2pt dataset/cosmolike arrays, probe-name reuse.
- [Sample efficiency is the goal](emulator-sample-efficiency-is-the-goal.md) — objective = smallest N_train to reach f(dchi2>0.2)<0.10 (real target T=512+w0wa+TATT makes N_train the binding constraint; "just add data" is dead). SHARPENED 2026-06-25: judge a lever by the CONVERGED PLATEAU of frac>0.2 at a FIXED useful N (~10k), not the small-N relative curve. A/B output-reparametrizations (RescaledChi2, ResidualBaseChi2) are CLOSED -- they share plain's optimum so a single converged run can't separate them (user saw A==B==plain ~0.20). Lower a DATA-limited plateau by reducing effective DOF (simpler target), not reparametrization/capacity/regularizers/loss-shaping. HARD constraints: no R change, no fine-tune, no cut, Gaussian-T fixed. Live levers: analytic INPUT feature (AugmentedParamGeometry, built-not-run) + per-bin ParallelResMLP.
- [Per-bin ParallelResMLP plan](per-bin-parallel-resmlp-plan.md) — FAILED 2026-06-25 (worse than single ResMLP, ~0.27 vs ~0.24), SUPERSEDED. A pure per-bin DENSE-MLP split threw away the shared cosmology map (30 heads re-learn it alone) AND a dense MLP is permutation-invariant so it never exploited within-bin smoothness anyway. Reusable machinery: BlockDiagonalGeometry (block whitening, chi2 keeps FULL Cinv), grouped batching (GroupedLinear / einsum "gbi,gio->gbo" / Conv1d groups=n_bins, the batch axis = GPU parallelism).
- [ResMLP + CNN architecture](resmlp-cnn-perbin-architecture.md) — CORRECTED 2026-06-25b: the clean global ResCNN is NEUTRAL at T=16 (f(dchi2>0.2) ~=0.20 == plain ResMLP, learned gate near init); the earlier "0.24->0.18 WORKED" was CONFOUNDED (changed full->diagonal whitening AND used a broken mid-nonlinearity-less CNNBlock). LEADING READ (user's call): the T=16 toy is too EASY/narrow to show the theta-structured residual a conv fixes -- the ResMLP already nails it (median 0.04), the leftover is the scattered tail, not axis structure; the CNN's published win (0.2->0.06) is at HIGH training TEMPERATURE (real target T=512). TEST by swapping a hotter training file (cs_64/256/512), rerun resmlp-vs-rescnn; watch gate grow + curve drop. Final arch = APPENDIX ResCNN (pure-ResMLP trunk in FULL whitening + gated conv correction converting to theta order internally via fixed buffers W_fd/W_df; CUDA-graph-safe). Build gotchas: CNNBlock needs act_mid (or 16 channels fold to 1); geometry-call-in-forward + reduce-overhead crash -> use buffers + compile_mode="default" in make_model (spec key, default reduce-overhead). Classes: DiagonalGeometry, CNNBlock, ResCNN, GroupedCNNBlock, ResCNNPerBin. PORT FIX 2026-06-30: ResCNN now threads --activation into the CNN head (block_opts['act'], fallback activation_fcn H); was silently H regardless of the trunk.
- [Emulator floor is data-limited](emulator-floor-is-data-coverage.md) — frac>0.2 floor is DATA (training-set size): the learning curve drops 0.219 (10k) → 0.100 (46k = goal) toward the 82k pool; baseline used only ~1/10. Fix = train on more data. A mid-session "it's capacity" call (from train==val underfitting) was WRONG, refuted by this curve — train==val rules out overfitting but NOT capacity; only the learning curve (metric vs N_train) settles capacity-vs-data.
- [Shared budget across sequential calls](shared-budget-across-sequential-calls.md) — splitting one allocator into N sequential calls vs a shared finite budget (VRAM/RAM) needs the running remainder threaded; by-value budget to sequential allocators = smell; finish resource accounting both directions.
- [No global variables in functions](no-global-variables-in-functions.md) — functions take params, read no data global; flag any unavoidable global with ⚠️ WARNING (never silent); audit free names with symtable. Caught run_emulator→device and stale param_geom.
- [Analytic-scaling preprocessing](analytic-scaling-preprocessing.md) — ratio preprocess the dv by R=xi_analytic(mid)/xi_analytic(cosmo) (E&H zero-baryon, linear, Limber); removes ~79% broadband variance. RESULT (narrow, one emulator): as a TARGET rescaling it LOST the sample-efficiency test (worse than plain at small N, converged by ~7k) — deprioritized. Mechanism = an UNCONFIRMED conditioning hypothesis (don't over-generalize). RescaledChi2 = the "A" form (R divides net output); ResidualBaseChi2 = the "B" form built 2026-06-24 to ISOLATE the conditioning (R moves only the baseline, plain chi2) — see [[geometry-loss-composition]]; the A/B difference is order-unity so B is the clean test, not an expected big win. Untested alternatives with a real mechanism: analytic as INPUT feature / PRETRAIN init (keep the plain loss).
- [Data staging: RAM + source dict](data-staging-ram-and-source-dict.md) — memmap the full dv dump; stage_source materializes the used subset to RAM if it fits 70% psutil.available, reindexing local (idx->arange) so the pipeline is unchanged; source dict carries C/dv/idx + C_mean/dv_mean; references not deep copies. ALWAYS build geometry from train_set["dv_mean"] / train_set["C_mean"] (the canonical source dict), NEVER the loose dvt_off / c_off globals — same value, but no drift to the wrong file. (Recurring slip: I keep writing dvt_off in construction cells.)
- [Activation generalizations](activation-function-generalizations.md) — the paper's H(x) = gamma·x + (1-gamma)·Swish_beta(x) (a learnable identity<->Swish interpolation; non-saturating = why it beat tanh), and three built generalizations: GatedActivation (multi-gate bulk, 3K+1 params), PowerGatedActivation (bounded tail exponent psi_p in [0.5,1.5], safe superlinearity, 3 params), GatedPowerActivation (both, 3K+2). Plus the conv-collapse gotcha (expand-then-collapse conv needs a mid-nonlinearity). Bake-off T=16 all NEUTRAL (H 0.244 / Power 0.240 / MultiGate 0.227 / Gated+Power 0.235, ~1.7sigma); the width sweep showed the MultiGate edge was CAPACITY not bias. Re-test at high T.
- [High-D scaling + TATT templates](emulator-high-d-and-tatt-templates.md) — emulator sample complexity scales with the EFFECTIVE (active nonlinear) dimension, not nominal param count; nuisances (photo-z DZ, IA A1) are ~0-hardness/cheap, so Roman n(z) bins add easy directions. The curse hits LOCAL methods, not a global net on a low-eff-dim smooth function (it beats local-linear 1000x). WHY TATT >> NLA: ~5 IA params enter as products up to quartic over a WIDE prior = coupled high-curvature wide-prior subspace. LEVER: the dv is EXACTLY a polynomial in the TATT amplitudes with cosmology-only smooth template coefficients -> emulate the ~10 templates K_t(cosmo,z), apply the TATT polynomial in CLOSED FORM (amplitudes leave the emulation, prior-width-independent), like A_s factoring out of C_ell. NEVER emulate a parameter dependence you can write down. Low-discrepancy helps coverage only low-D (curse high-D); active learning needs concentrated error (diffuse failures -> representation lever, not sampling). NOW IMPLEMENTED -> [[npce-and-ia-template-factoring]].
- [NPCE + IA template factoring](npce-and-ia-template-factoring.md) — 2026-06-26 BUILD + PICKUP POINT. (1) NPCE (PCE base + NN refiner, the user's paper) is NOT useful for cosmic-shear xi: a PCE only adds CAPACITY, can't lower the data floor (a smoothness prior helps only an under-smoothed model; the ResMLP is already smooth; PCE nails the easy amplitude mode, wrong on the non-low-degree shape modes; NPCE-128 0.208 == ResMLP-256). PCE build lessons: low degree (Runge), LOO-gate modes (a wiggly base poisons the refiner), few leading modes + refiner backstops, early-stop the mode loop. (2) GG+IA TEMPLATE FACTORING (the live lever, where we FINISHED): dv is EXACTLY a polynomial in the IA AMPLITUDES with cosmology-only template coeffs -> emulate templates, apply the polynomial in the LOSS, amplitudes never enter the net (exact, prior-width-independent, free amplitude prior). Implemented as ARCHITECTURE on the EXISTING scattered samples (no extra sims/extraction, read amps per-sample). Classes IAFactorGeometry/IATemplateMLP/IATemplateChi2 + nla_coeffs/tatt_coeffs. Amplitude/power split (amplitudes factor, eta powers stay). NLA NEUTRAL on the toy (A1 easy/narrow prior) = validation; the win scales with prior width = God-given for TATT. NEXT: high-T TATT dataset to stress-test, then [[notebook-to-python-translation]].
- [Notebook -> Python translation TODO](notebook-to-python-translation.md) — STRUCTURE NOW DONE 2026-06-29 -> [[emulator-python-package]]; the two rules still hold for remaining cells (double-check docs; VISIBLE WARNING on not-yet-understood pieces). Original: translate pytorch1.ipynb into standalone Python CLI files, dataset_generator_lensing.py style (argparse one --flag per option; YAML train_args; ROOTDIR-relative).
- [Emulator Python package + drivers](emulator-python-package.md) — SESSION 2026-07-04 block = the CURRENT interface (nested model schema, 3 architectures × ia, two-phase + symmetric trunk:/head:, warmup-bug fix, baseline best-seeding) + RESUME STATE (rerun two-phase, restrf first runs, positional-encoding discussion, full-tree cocoa re-sync). Earlier: 2026-06-29: the notebook section IS now a package emulator/ (flat modules + parallel/ PCE/ IA/ subfolders; the folder carries the qualifier, so files inside drop the suffix) + CLI drivers driver/ (dataset_generator_lensing-style, sys.path bootstrap, --yaml with data+train_args). BYTE-FAITHFUL port (extract from .ipynb JSON, scope to section cells>=240 to dodge duplicates, dedup, verify ast+binding+unused+keyword). New helpers in training.py: build_run_specs (config->the 6 run_emulator spec dicts, KEYED for **splat), pick_device, the [default,min,max,kind] search-range resolvers (default/suggest/search_train_args); make_chi2 in loss_functions; make_activation; load_source; the diagnostics module. Two drivers: train_single + tune_single (Optuna); ONE YAML serves both (ranges collapse to defaults for training). UPDATED 2026-06-30: EmulatorExperiment setup-object (emulator/experiment.py; from_yaml/from_config + raw_train_args; stage_train(n_keep)/stage_val/build_geometry/build_specs/train(silent)/run/frac_above/pool_size; MODELS registry + DEFAULT_THRESHOLDS) — both drivers refactored onto it; drivers RENAMED (drop "resmlp"), model chosen in YAML train_args.model.name; sweep drivers sweep_ntrain + bakeoff_activation (activation×N double loop); results.save_learning_curves = plain-text np.loadtxt columns (NOT json); plot_learning_curves; load_source n_keep (nested subsets); make_logger. UPDATED 2026-07-01: sweep_ntrain + bakeoff now MULTI-GPU (torch.mp spawn, one process/GPU; new emulator/scheduling.py lpt_assign; ram_frac=0 streams the shared dump) -> [[multi-gpu-sweep-pattern]]; README.md at repo root (cocoa-style: nested TOC + ASCII pipeline diagrams + per-file appendices); all non-hot comprehensions converted to explicit C-style loops; serial paths inlined into the n_workers<=1 branch (helpers dropped); whole tree compiles clean. UPDATED 2026-06-30b: drivers MOVED out of driver/ to the package root (beside emulator/) → no sys.path bootstrap (launch-by-path puts the script dir on sys.path[0]); new emulator/cocoa.py = the --root/--fileroot/--yaml cocoa CLI (data block = bare filenames under --root/chains); deploy path .../emultrf/dev/; run from $ROOTDIR; example YAMLs → example_yamls/ (copy-from templates); README synced. UPDATED 2026-07-04y: FIVE drivers now — + sweep_hyperparam (one YAML-chosen train_args leaf via a sweep: block, dotted path + values; activation special-cased; own example YAML with 5 swap-in sweeps) — all hyperparameter drivers multi-GPU (shared run_gpu_pool in scheduling.py; tune via optuna JournalStorage --n-gpus/--journal, same journal resumes); --gpu-pack VRAM-token co-location on both sweep drivers (off by default); README section 6 has the sweep-block + multi-GPU/packing subsections; startup banner = EmulatorExperiment.print_design(), shared by all drivers.
- [Multi-GPU sweep pattern](multi-gpu-sweep-pattern.md) — 2026-07-01: single-node multi-GPU parameter sweep. TASK-parallel (one whole training/GPU, NOT DDP); PROCESSES not threads (GIL; cosmolike ci global C state; EmulatorExperiment caches on self); torch.mp SPAWN not fork (CUDA can't cross a fork; worker fn module-level, build exp inside); set_device(k) per worker + del-refs & empty_cache between points so run_emulator's mem_get_info sizes the right card. Balance: LPT for sweep_ntrain (lpt_assign in emulator/scheduling.py, plain min-scan, no heapq) since cost~N; ACTIVATION-split for bakeoff (equal cost, no LPT, every GPU runs all N). Long-pole = single biggest job. Host RAM: dv dump is a SHARED memmap but stage_source's np.asarray(dv[rows]) makes a PRIVATE per-worker copy -> ram_frac=0 in the parallel path (the [[shared-budget-across-sequential-calls]] trap in PARALLEL form; materialization = false optimization in multi-GPU, legit single-process). Streaming is per-CHUNK (load=bs*batches_per_load) not per-minibatch. Serial fallback when n_workers<=1 (Mac/MPS); refuse pure CPU. 50M-row wall = np.loadtxt of the param file per worker. UPDATED 2026-07-04y: shared run_gpu_pool (spawn pool, per-GPU queues + lanes, setup_fn/job_fn, timeout+liveness drain) now under BOTH sweep drivers; --gpu-pack VRAM tokens (<=20% -> 4/GPU, <=40% -> 2, else exclusive; conservative 2*N*W*4 + 2GiB estimate; off by default; works on 1 GPU); PY3.14 TRAP: Process releases args after start() -> loop-created sync objects GC'd -> children die in SemLock._rebuild -> pool keeps a keepalive list until join; tune_single parallel via optuna JournalStorage (one worker/GPU, per-worker sampler seeds, same journal resumes).
- [Test workstation GPUs](test-workstation-gpus.md) — the user's NVIDIA TEST rig (3rd machine, vs the Mac [[dev-machine-mac-m2-32gb]] + the NVWULF 8xH200): 2x RTX 3060 12GB, driver 535 / CUDA 12.2. GPU0 (PCI 09:00.0) = an eGPU, SHARED (another user 'evan' runs cocoa there); GPU1 (PCI A1:00.0) = the internal/display GPU (Xorg+gnome-shell) they want to DEFAULT to. Asked 2026-07-01 how to default to GPU1 / avoid the eGPU while testing all drivers. ANSWER: CUDA_DEVICE_ORDER=PCI_BUS_ID (CUDA's default 'fastest-first' order can differ from nvidia-smi's PCI order, so '1' may not be the GPU you think) + CUDA_VISIBLE_DEVICES=1 (only GPU1 -> becomes cuda:0; sweep/bakeoff see device_count==1 -> SERIAL path) OR =1,0 (both, GPU1 first as cuda:0, eGPU as cuda:1 -> the multi-GPU dispatch runs). First-listed device = cuda:0 = default; set it BEFORE the process starts (shell/rc, not post-import-torch); drivers respect the remap (pick_device/device_count/set_device).
- [omegam^2 h^2 window cut](omegam2h2-window-cut.md) — 2026-07-03, first T=256 run (frac>0.2=0.59@250k): new physical cut 0.015 < omegam²h² (=Γ²) < 0.08 implemented (phys_cut_idx omegam2h2_lo/hi, YAML keys); coverage-framed (divisor draws from cut pool → densification); sparsity+failures U-shaped along the product; hardness itself ∝ Om h^2.2 (PDF-forensics fit); WARNING: keep=n//divisor is pre-cut → val_divisor 2 on cs_128 may trip "pool too small". OUTCOME: frac>0.2 0.59→0.156, median 0.30→0.053, 17-min runs (bs 768); residual failures hug the cut boundary + diffuse (R² 0.35→0.18, sparsity R² 0.01 → coverage isotropic in-window); A1_1 emerging; next = N_train sweep (only 250k of ~1M pool used). LIVE production cuts (user-confirmed): omegabh2 ∈ (0.005, 0.035) [chose lo 0.005, looser than suggested 0.014] + omegam2h2 ∈ (0.015, 0.08). An omegabh2_lo cut also exists in code; NLA design wired as model.name: nla (AmplitudeFactorGeometry + TemplateMLP + TemplateFactoredChi2/nla_coeffs, LSST_A1_1 factored).
- [nla_as design spec + rescnn_nla build](nla-as-design-spec.md) — nla_as ABANDONED + code DELETED 2026-07-04 (Ãs amplification, the A-form tax; carry mechanism removed too; only the lesson survives). CONFIG REDESIGN 2026-07-04: model.name = architecture ONLY (resmlp|rescnn) + separate model.ia key (None|nla|tatt-reserved); MODELS keyed by (name,ia); IA_DESIGNS dict; old names error. ParallelResMLP + grouped-ResMLP blocks DELETED (parallel/ keeps ParallelResCNN + GroupedCNNBlock only). rescnn+nla BUILT + SPEED FIXES: head is BANDWIDTH-bound (s/epoch ∝ channels × n_blocks_cnn); templates-as-conv-channels is THE head (fold path deleted), zero-init identity head, trunk_epochs two-phase (phase 1 = pure nla cost, phase 2 = frozen no_grad trunk) + train_args.head override block (lr_base/loss_mode/trim/focus). ResTRF BUILT 2026-07-04 + ADAPTERS DELETED 04h (no embed/out — the paper's heaviest layers existed only for its synthetic latent sequence; tokens = raw padded bin segments at NATURAL width, nla = (template,bin) pairs = 90 tokens; width knob GONE, n_heads must divide max_bin (26→1|2|13, default 2); TRFBlock == identity at init via zeroed branch outputs, corr = blocks(h)−h; head halved ~110k nla; trf.shared_mlp ablation flag). HANDOFF-JUMP BUG FIXED 2026-07-04j (user's 300+700 test): warmup lr was applied AFTER each epoch → epoch 1 of every pass trained at FULL base lr (wrecked the phase-2 identity start); fixed (warmup at top of epoch) + baseline epoch-0 eval now seeds best-tracking (a pass can never end worse than it started). Advice: phased runs → gate_init 0.1 + small annealed head trim until the trunk is mature. TO DISCUSS next session (user deferred, tired): POSITIONAL ENCODING in the TRF (shared_mlp mode is position-blind; learned per-token embedding ~2.3k params would make it a clean ablation) + whether position matters generally across the designs — full context in the note. CONV HEAD REDESIGNED 2026-07-04d: bins-as-channels single kernel (Conv1d G→G, nla T·G→T·G; channels knob + CNNBlock + TemplateMixCNNBlock DELETED; tensors never exceed padded dv size → bandwidth problem gone by construction). NESTED MODEL SCHEMA 2026-07-04e: model block nests mlp/activation/cnn/trf sub-blocks (short keys, no suffixes; MODEL_BLOCK_KEYS translation, loud errors on flat/unknown/misplaced keys; n_heads hardwired). Scoreboard STALE (user 07-04: current runs are smoke tests; real test = TATT+w0wa sample efficiency). 07-04 blocks 04k-04s: conv-as-matmul tried+REVERTED (CPU-only pathology; params != time on the small-SM GPU); first full run 0.1105 then chi2-tail collapse post-mortem (04l); clip + rewind guards (04m, rewind production-verified); rescale_kernel (04n); trunk-vs-head param print (04o); groups channel cuts 2|3|6 validated vs pm_kept (04p); separable depthwise+pointwise (04r; param economy NOT speed); head focus enabled kappa 0.2 + current production template + YAML block style + README = first-class doc surface (04s); compiled fwd_loss + eval twin + pre-shuffle (launch-bound fix, 04t; kappa CPU-lift = primals_90, fixed+confirmed); film BOTH heads + TATT LIVE awaiting dumps (04u/04v); transfer-learning curriculum idea + strong first evidence (04w); post-compact: print_design shared banner + driver-doc parity + parallel//PCE currency (04x); sweep_hyperparam driver + run_gpu_pool + --gpu-pack tokens + parallel-Optuna journal (04y).
- [Cocoa ROOTDIR env var](cocoa-rootdir-env.md) — ROOTDIR (the driver/ scripts' os.environ anchor) is NOT in the README; it's `export ROOTDIR=$(pwd -P)` in Cocoa/set_installation_options.sh = abs path of Cocoa/, sourced via start_cocoa.sh each session; anchors .local/projects/external_modules.
- [.py module style conventions](py-module-style-conventions.md) — for emulator/ + driver/ ONLY (NOT the slide notebook): NAMED PARAMETERS everywhere allowed ("I forget the meaning of position X"); irreducibly-positional args (matplotlib x/y, einsum operands, cat/from_numpy/asarray/zeros subject, model(x)) stay positional + a naming COMMENT; PAREN-ALIGNMENT (OVERRIDES [[hanging-indent-not-paren-alignment]] for .py); 90-col; didactic comments on shape ops (unsqueeze/expand/[:,None]/view/searchsorted) incl. the geom.unsqueeze(scatter)-vs-torch.unsqueeze(add-axis) name-clash. Keep *args forwarders positional (else "multiple values"). DOC QUALITY (2026-06-30): module docstrings = PROSE (real subjects, not slash-as-subject lists); PS: jargon defs at file end (whitened/encoded/resident/loader/dump/memmap/Mahalanobis); NO double-dash; blank-line-group dense method bodies (leave aligned = tables); cross-module call sites get a (module.py): what-it-does provenance comment; enumerate EVERY block-dict key in the docstring; comment math as an explicit display formula with named symbols. MORE (2026-07-01): NO comprehensions in non-hot code -> explicit C-style loops (keep vectorized numpy/torch math; AST-scan to find them, grep misses multi-line); paren-align one-per-line covers DICT pairs + positional tuples; INLINE single-use trivial helpers; doc-compression FLOOR (a whole-package 8-subagent pass landed ~9.5% overall, ~3-8% lean modules / ~15% driver headers; can't trim 20% without cutting points); README cocoa-style = nested TOC + per-file appendices + ASCII flow diagrams (user loves the diagrams). SHAPE-FLOW DIAGRAMS (2026-07-04, "I love this SO MUCH"): vertical │/▼ tensor-pipeline graphs in docstrings, EVERY symbol defined in a trailing (legend: ...) — undefined symbol = graphical magic number; rolled out package-wide (designs, IA, losses, geometries, training, experiment); format spec also in the pytorch-teaching-style SKILL + auto-memory.
- [Whole-package style/doc audit (2026-07-05)](audit-package-style-2026-07-05.md) — Architect whole-tree audit of emulator/ + 5 drivers + README + example_yamls: PASSES structural axes (no data-global reads, keyword names valid, probe generalization, shared-budget threading, weight-decay split, driver flag parity); 29 fix items across P1-P6 (dead NLATemplateMLP/NLAInputGeometry deletion, training.py docstring drift, plot_xi bugs+gist_rainbow, README one generation behind, ~50+ caps + ~315 double-dash + 7 legend-less diagrams, PS gaps, PCE Arguments retrofit). IMPLEMENTER RESUME + Architect re-audit + Implementer DELTA all appended 2026-07-05 (Opus, worktree amazing-keller). First pass: all items except 27; gates G1-G9 pass. Architect re-audit -> DELTA D1-D6 (de-caps NON-AMPLITUDE; define 0.8 headroom; driver+straggler PS jargon; de-caps/de-dash the now-sanctioned string literals; finish P5 Arguments/provenance on loss/geometry/PCE/experiment). DELTA DONE except D6 (item 27 + runtime import, WORKSTATION-only): caps/dash now 0 incl. string literals, every param method in the D5 files has Arguments, G9 AST-diff = only sanctioned edits. One flagged residual: experiment.py major calls keep 2-space hanging indent (paren-align would break the 90-col gate). Branch merge-ready pending D6. Re-audit THIS branch, not sleepy-lumiere's stale copy.
- [FiLM conditioning](film-conditioning.md) — recovered 2026-07-04 from a months-old claude.ai chat (user re-shared the PDF): FiLM = per-channel gamma(z)*h + beta(z) reinjection of cosmology into the CNN blocks; maps onto our heads as a parameter-DEPENDENT generalization of the gate ((B, T*n_bins) from the NON-amplitude params only — amplitude exactness constraint); identity init matches house style; ~2.2k params/block; IMPLEMENTED same day on BOTH head families (model.cnn.film / model.trf.film, identity-init, amplitude-blind); first evidence strong (truncated trunk 0.50 -> 0.277 where film-less stalled ~0.37); remaining ladder: film_grouped, conditional LayerNorm.
