# Execution backlog — the countable ledger

EMERGENCY MANDATE 2026-07-14 (USER, temporary — Fable and Opus out of
credits): Codex holds specification, implementation, independent gate
re-execution, adjudication, landing, and backlog-reconciliation authority for
this recovery. The user's later direct instruction expressly authorizes
one-unit merges to `main` and pushing `main`; it supersedes mailbox 0166's
earlier `push stays denied` clause. Scientific contracts, gate integrity,
reviewable landing granularity, and the main-hygiene regime remain binding.

One line per OPEN unit of execution work, maintained by the Architect.
The mailbox daemon counts the "- OPEN" lines here PLUS the queued
mailbox messages; that total is the queue depth the second-Implementer
threshold (SECOND_IMPLEMENTER_THRESHOLD = 10) compares against — at or
past it, the red team is the second Implementer and build units flow
to both lanes. A unit leaves this
list when it lands and its audit records GO — not when it is
dispatched. Details live in the notes each line names; this file is
deliberately just countable lines.

- CLOSED loop documentation: `notes/README.md` is now role-first and visual, with stable role boundaries, selectable `--architect-model` / `--implementer-model` bindings, Architect-only GO/NO-GO authority, named-delta Red Team scope, a project-local accessible SVG, shorter paragraphs, collapsible exact references, and Mermaid safe-stop/lane graphs. Independent named-delta audit GO: output-style parity 8/8; help/fix-only parity 14/14 with 20/20 mutations killed; role-model routing 5/5 with 5/5 mutations killed; safe-stop 9/9 with 7/7 mutations killed; exact help and `build_agent_commands()` blocks, internal anchors, balanced fences/details, rendered SVG, XML, and whitespace checks all pass — USER 2026-07-14
- CLOSED emulator organization: all four `emulator/study_*.py` modules now live in `emulator/studies/{implementation,manifest,manifest_digest,name}.py` (USER, 2026-07-14) — the repeated prefix and old flat paths are gone with no compatibility duplicates; production, gate, wrapper-comment, and package-map references use the package namespace. Independent named-delta audit GO: the unit-53 manifest witness is ALL PASS including the exact-package/no-flat-duplicate arm, all 44 unittests pass, board import closure lists successfully, py_compile/import probes are green, and scientific functions, constants, fields, digests, and journal behavior are unchanged — notes/gates-and-board.md "Study support package move audit" 2026-07-14
- CLOSED mailbox tools: configurable Claude role models — `--architect-model` and `--implementer-model` independently select Claude aliases or full model IDs while the legacy `to-fable` / `to-opus` addresses remain stable Architect / Implementer routes. Defaults preserve Fable / Opus behavior; the expected `--architect-model opus --implementer-model sonnet` launch is proven without changing Sol, effort, context, safe-stop, or mailbox semantics. Invalid names stop before claim or launch. Independent named-delta audit GO: 5/5 focused runtime arms, 5/5 mutations killed, and every preserved daemon suite green — notes/gates-and-board.md "Daemon configurable Claude role-model audit" 2026-07-14
- CLOSED unit 84 (artifact chain, fixed-facts adapter half): audited PASS and committed d3b9289 2026-07-14 — notes/gates-and-board.md units 84+85 audit; rider repair (schema-v2 gate fixtures) dispatched 0161
- CLOSED unit 85 (artifact chain, fixed-facts adapter half): audited PASS with unit 84, same commit and audit entry
- OPEN unit 96 core: persist and validate the required native artifact composition mode (`plain`/`npce`/`transfer`, plus native refined fact), with two-way required/forbidden-group checks before construction; absence must not mean plain — ruled contract in notes/gates-and-board.md:3889 and notes/artifacts-inference-warmstart.md:787. The narrower const-mask declaration rider is closed separately below; it does not implement this enum.
- CLOSED unit 96 const-mask declaration rider: current saves derive an ordered-mask SHA-256 declaration for the main Grid2D geometry and embedded transfer base; rebuild validates it before geometry/model construction, refuses declaration absence on schema-v3 and any paired mask/declaration on non-Grid2D, and explicitly limits the claim to one-surface integrity. Independent audit: 10/10 const-mask tests and all 7 MPS identity AIDs green; moved-pin/count-only and validator-bypass mutations red — notes/gates-and-board.md "Unit 96 const-mask declaration rider audit" 2026-07-14
- CLOSED unit 94 (boundary-interior half): audited GO at a0a03a9 and ported to main as f046085 on 2026-07-14; current-main witness 12/12 green and all four mutation controls red at 4/5/1/7 — notes/red-team-audit-and-didactics-2026-07-13.md "Unit 94 current-main landing verification"
- OPEN unit 8: UNBLOCKED 2026-07-14 (unit 94 audited GO) — dispatched to the Implementer 0163-to-opus; step 1 merges a0a03a9 into the record branch (zero-conflict expectation), then the identity manifest digests the resolved support — notes/state-2026-07-11-and-next.md + the unit-94 GO entry
- OPEN unit 24: see state note
- OPEN unit 56: resume machinery — kept, see state note
- OPEN unit 62 (D5 remainder): see didactics register
- OPEN units 64/70 (D5 remainder): see didactics register
- OPEN staging reopen (unit 32 successor): needs Architect ruling
- OPEN unit-93 hold resolution (83e4507)
- CLOSED daemon: --dry-run is read-only even for malformed or placeholder messages; the exact placeholder file remains in the pending root and no failed/ state is created, while real dispatches claim inflight/ atomically — notes/gates-and-board.md "Current-daemon transport safety audit" 2026-07-14
- CLOSED daemon: user-facing terminal output now uses semicolon separators and sentence-case emphasis while preserving protocol/acronym literals and the binding PREAMBLE; the README's quoted heartbeat and demand-hint lines match runtime exactly. The focused witness is 8/8 green, the existing daemon reproduction is 8/8 green, and separator/all-caps mutations turn the focused witness red — notes/gates-and-board.md "Daemon terminal-output register recovery audit" 2026-07-14
- CLOSED unit 53-REPAIR: one canonical manifest now authenticates stable family naming, fixed/search/objective choices, complete scientific inputs, runtime-resolved CosmoLike values, and a versioned unit-37 semantic implementation registry before enqueue, spawn, staging, or winner reporting. Loaded legacy studies refuse, workers rebuild identity, failed workers cannot crown old trials, and the manifest-owned default is queued exactly once. Both independent audits report GO on the tensor-repaired snapshot: Cocoa Torch witness 34/34, real shipped thresholds canonicalize, all 10 critical/coupling mutations red, exact env-only n_theta drift refuses, compile/whitespace clean; real Optuna journal smoke remains environment-owed — notes/gates-and-board.md "Unit 53 current-main repair landing audit" 2026-07-14
- CLOSED gate defect: generator_ranges now asserts the generator-owned rows-only sidecar contract, so the exact retired header is caught whether GetDist accepts or rejects comment rows; decimal-format mutation remains load-bearing — notes/gates-and-board.md "Gate-integrity pair recovery audit" 2026-07-14
- CLOSED gate defect: transfer-identity.cross-family-base-refusal now supplies a complete data fixture and reaches the exact cross-family law; deleting n_train/n_val independently turns only that aid red at the former early failure — notes/gates-and-board.md "Gate-integrity pair recovery audit" 2026-07-14
- CLOSED staging defect: numeric-chain dumps such as <paramsf>.1.txt now resolve and enforce the shared <paramsf>.paramnames sidecar through one resolver used by ordinary, facts, and scalar staging; 6/6 focused tests and stage-ram are green, while deleting the numeric-root fallback turns the focused witness red — notes/gates-and-board.md "Numeric-chain .paramnames recovery audit" 2026-07-14
- CLOSED unit 90 rebase: audited implementation 50f1c63 is already an ancestor of main through ce99f87; stale ledger line retired during the 2026-07-14 recovery reconciliation — notes/gates-and-board.md UNIT 90 verdict
- CLOSED unit-13 covariance package (25M-08/11/12 + 45M-01): substance tip 2fd8a9d plus wiring delta 7583019 independently audited GO and ported to current main in the commit carrying this line; all five CPU witnesses green and each of the four validator-removal mutations reds its named main() refusal; Torch cmb-identity + real-CAMB byte-identity remain workstation-owed — notes/families-scalar-cmb.md, "Unit-13 wiring delta and current-main landing audit"
- CLOSED daemon: the dispatch prompt and all five ruled wording surfaces now preserve the ordinary notes-first outbound while exempting only an explicit binding TERMINAL/no-reply inbound; ambiguity requires an outbound. Four focused prompt/surface regressions and the existing eight-arm daemon reproduction are green, and removing either the ordinary imperative or terminal exception reds its focused arm — notes/gates-and-board.md "Conditional terminal-preamble recovery audit" 2026-07-14
- CLOSED daemon: every live dispatch now carries one post-claim mechanical store-max/newer-same-CWD currency snapshot without making a semantic skip decision; timeout-only atomic history supplies the exact prior-kill retry sentence; and guarded exact-inode done/failed publication, persistent inflight lane blocking, and truthful `--once` propagation prevent an ambiguous head from being consumed or releasing later shared-tree work. Invalid thresholds launch nothing, hostile histories refuse conservatively, dry-run is read-only, PREAMBLE and raw LF/CRLF body suffixes are preserved. Two independent final audits report GO: 18/18 focused runtime arms, 9/9 source mutations killed, preserved daemon 8/8, output-style 8/8, and conditional-preamble 4/4 — notes/gates-and-board.md "Daemon dispatch-currency, timeout-history, and archive-truth audit" 2026-07-14
- CLOSED reader defect: rebuild now re-proves the schema-v3 record's sampled-name order against the independently rebuilt input geometry before model weights load; the coordinated blocks+sidecar rewrite witness is refused, its matching control advances, and bypassing the new comparison reaches torch.load and turns the witness red — notes/gates-and-board.md "Rebuild-time fixed-facts name audit" 2026-07-14
- OPEN tools-review daemon-repair unit: CORE RECOVERY + CONDITIONAL PROMPT + OUTPUT STYLE + CURRENCY/ARCHIVE TRUTH + DEAD-MAILBOX WARNING + FIX-ONLY + GLOBAL RENDEZVOUS LANDED — the non-daemon router/finite slice, current-daemon safety port, conditional terminal/no-reply prompt, terminal-output register repair, dispatch currency/timeout/archive truth, literal live-watch send diagnosis, closing-only Sol guard, and audited global safe-stop rendezvous are on main with their focused witnesses green. Only the separately listed landing-debt rider remains open — notes/gates-and-board.md "Non-daemon relay-tools recovery audit" + "Current-daemon transport safety audit" + "Conditional terminal-preamble recovery audit" + "Daemon terminal-output register recovery audit" + "Daemon dispatch-currency, timeout-history, and archive-truth audit" + "Daemon dead-mailbox send-warning audit" + "Daemon Sol-ticket deferral and fix-only watch audit" + "Daemon global safe-kill rendezvous and bounded role-language audit" 2026-07-14
- CLOSED daemon: `--send` and `--ping` now warn after a successful publication (or dry-run would-queue line) when their own mailbox lacks an exact held `watch pid N` lock, name that mailbox, and list every other live watched main/Claude-worktree mailbox in sorted order without rerouting or failing the send. Read-only shared nonblocking probes distinguish watch from once/stale/legacy/malformed state; bounded exact owner parsing, device/inode rechecks, and final/parent/base symlink refusal keep diagnosis conservative. Dry-run creates nothing. Two independent final audits report GO: 9/9 focused runtime arms, 7/7 source mutations killed, preserved daemon 8/8, output-style 8/8, conditional-preamble 4/4, and staleness/archive 18/18 + 9/9 — notes/gates-and-board.md "Daemon dead-mailbox send-warning audit" 2026-07-14
- CLOSED daemon: public `--send sol` now requires and persists exact `closure|discovery` classification, refuses malformed or saturated discovery with the END/below-threshold instruction, and preserves the admitted demand-9 candidate at dispatch. Truthy `--watch --fix-only` binds child and external sends through banner/environment plus an activation-serialized, exact-inode, fail-closed per-mailbox mode lock; only declared closures and the byte-exact internal transport ping launch. Ambiguous primary actions fail. Two independent final audits report GO: 14/14 focused runtime arms, 20/20 source mutations killed, dead-mailbox 9/9 + 7/7, red-team 8/8, staleness 18/18 + 9/9, output-style 8/8, and conditional-preamble 4/4 — notes/gates-and-board.md "Daemon Sol-ticket deferral and fix-only watch audit" 2026-07-14
- CLOSED daemon: global safe-kill rendezvous — the watch globally stops new admissions after K=5 completed child turns or M=15 monotonic minutes in an uninterrupted busy epoch, waits for every admitted preparation and live child, prints the main-thread 20-line 19..0 safe countdown with a freshly recomputed queue count, expires the safe status before reopening, and treats the ordinary 20-second idle poll as an explicitly bounded safe opportunity. Pre-claim and live-child statuses remain explicitly unsafe; Ctrl-C during the window preserves queued bytes, source edits stop the next mid-pass admission, and `--once`/`--dry-run` keep their finite call shapes. Independent audit GO: 9/9 focused runtime arms, 7/7 boundary mutations killed, and every preserved daemon suite green. The same bounded audit records GO for the user's Architect GO/NO-GO vocabulary and named-delta-only Red Team scope — notes/gates-and-board.md "Daemon global safe-kill rendezvous and bounded role-language audit" 2026-07-14
- OPEN daemon: landing debt must self-correct (USER DIRECTIVE 2026-07-14: "I just need to make sure the code does that automatically because this night it waited 8 hours ... a commit with 12,000 lines"): three mechanical legs, none relying on Architect discipline — (a) the PREAMBLE of every fable-lane dispatch carries the standing order: an audit turn that records GO performs the squash landing ITSELF in the same turn (grant on record) after the pre-squash foreign-commit walk, and the walk is a STOP, not a printout: any unaudited foreign commit aborts the squash; (b) every watch pass measures landing debt (git diff --shortstat main..branch) and past LANDING_DEBT_LINE_LIMIT (400) it does not merely hint — it WRITES an automatic fable-lane message ordering a landing-only turn (audit whatever is unaudited, land unit by unit), deduplicated so at most one such message is ever queued; (c) the debt line prints in EVERY demand report so the human sees the number rise before it becomes a batch; code edit deferred to an idle watch — rides the tools-review daemon-repair series
- OPEN v1.0beta1 hygiene prerequisite — main-branch + GitHub hygiene (USER DIRECTIVE 2026-07-14; ordered after all substantive/fix units but before the FINAL AI-development tree consolidation below; this unit's GO does not authorize a tag): the public repo must stop seeing loop churn — (a) main commits are ONLY accepted real fixes: bug dispatched, return accepted, ONE squash commit (no intermediate steps, no rules-tweak landings, no ledger-update landings); (b) notes/backlog.md goes on .gitignore (`git rm --cached` since it is tracked today) — the user still reads it from the local main checkout, GitHub never sees it; (c) the loop-narration notes leave GitHub the same way (red-team-audit-and-didactics.md, red-team-implementer-handoff.md, state-2026-07-11-and-next.md and kin — anything narrating the agent loop rather than the library); (d) at most 10 tracked markdown files remain, each stating a GENERAL property of the library, and a bug fix touches them only when it fundamentally changes a property they state; (e) the ledger-hygiene same-turn rule keeps operating on the now-ignored file (branch commits fine; a ledger edit is never by itself a main landing), and — AMENDED same day, live IMMEDIATELY — every edit to a LOCAL-ONLY note — this ledger AND the loop-narration set of leg (c) (gates-and-board.md, the red-team registers, the state/incident notes) — is mirrored by `cp` onto the MAIN checkout at EVERY TICKET CLOSE (the GO turn that retires a ledger line mirrors in the same turn; intermediate edits may batch until then — cadence RULED by the user 2026-07-14): the user checks the loop via main, not the worktree, and the gitignore will stop merges from carrying these files (rule codified in FABLE_ROLE.md, Ledger hygiene); (f) FABLE_ROLE.md landing doctrine amended to match; the Architect drafts the <=10 keep-list for user sign-off BEFORE any `git rm --cached` runs; (g) AMENDED same day (and AGAIN: doc now, tense-safe — dispatched 0164-to-opus, queued after the 0156 README trim in the same lane): notes/README.md — the current path, moved without duplication to ai/README.md by the final unit below — documents what a main commit is (one squash per accepted fix), which files are local-only (the ledger + loop-narration notes, ignored, readable from the local checkout), and the <=10 tracked-markdown rule, written to stay true after the tag next to the existing landing-doctrine passage
- CLOSED README didactic rewrite — `notes/README.md` now teaches one ticket progressively, separates replaceable models from stable roles and grant-controlled git authority, gives runnable `python3` recipes with expected outcomes, maps the current tree to the physical-last `ai/` layout, and replaces procedural prose with the role SVG plus seven sequence/flow/state diagrams. Six disclosure blocks hide operational depth, no ordinary paragraph exceeds 55 words, and the exact help, heartbeat, demand hint, safe-stop, and shipped command-builder contracts remain byte-identical. Independent didactic and contract audits report GO; output-style 8/8, fix-only 14/14 + 20/20 mutations, role-model 5/5 + 5/5 mutations, rendezvous 9/9 + 7/7 mutations, anchors, links, fences, SVG, and diff hygiene are green — USER DIRECTIVE 2026-07-14
- OPEN FINAL AI-development tree consolidation — AI-only support surface (USER DIRECTIVE 2026-07-14; immutable physical-last sentinel: do not dispatch until every other OPEN line above, including the v1.0beta1 hygiene prerequisite, is CLOSED with recorded GO; newly discovered work is inserted immediately above this line): at a verified global safe-stop with every watcher stopped and no pre-launch or inflight work, preserve ignored mailbox/relay state and rename `notes/README.md` to the AI-development landing page `ai/README.md`, then move `tests/` to `ai/tests/`, the remaining `notes/` to `ai/notes/`, `gates/` to `ai/gates/`, and `tools/` to `ai/tools/`; leave no compatibility copies, wrappers, symlinks, duplicate README, or old root directories. Update every operational locator and current documentation link in the same unit: imports/module names and dynamic loaders; repository-root derivation (including the daemon, router, board, 27 moved checks, and 10 topology-aware tests); subprocess and shell commands; daemon mailbox, relay, watch-state, lock, archive, demand, and main-mirror paths; gate configs, registries, path-prefix logic, logs, evidence-note resolution, and workstation commands; test discovery, fixtures, scratch topologies, snapshots, executable modes, and exact CLI/help expectations; `.gitignore`; `CLAUDE.md`, `.claude/`, and `.codex/`; root/package/gate READMEs, active comments/docstrings, example YAMLs, TeX guide plus regenerated PDF, and live Markdown links. `ai/README.md` becomes the single AI-development entry point and documents the layout, launch recipes, tests, gates, tools, local-only notes, and migration. Independent audit GO requires an exact filesystem and `git ls-files` census proving the new layout and absence of old roots, duplicates, wrappers, and symlinks; preservation of the four executable test modes and the unrelated incident-note working-tree delta; a stale-operational-path scan; focused root-resolution mutations proving no old-path or caller-CWD fallback; compile/import closure; every locally runnable unittest and daemon/router reproduction from `ai/tests/`; and successful `python ai/gates/run_board.py --list`, `--check`, and `--dry-run`, `python ai/gates/checks/board_selftest.py`, `python ai/tools/mailbox_daemon.py --help` and `--dry-run`, and `python ai/tools/handoff_router.py --status`, with hardware-only gates explicitly recorded rather than silently waived. This is the one final pre-release squash commit: hygiene GO alone does not authorize `v1.0beta1`; only after this commit is on `main`, independently audited GO, the local ledger is reconciled at `ai/notes/backlog.md`, and `main` is pushed does the user tag that exact commit `v1.0beta1`.
