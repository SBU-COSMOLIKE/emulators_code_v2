{
  "_help": {
    "what": "Deployment paths the workstation gates need. The harness reads only this file for the run's --root/--fileroot, the yaml directory, and the saved-emulator roots; nothing is hardcoded to one machine. Edit the values once on the workstation after `git pull`, then run `python gates/run_board.py --check`.",
    "rootdir": "Absolute path to the cocoa $ROOTDIR (the clone root that owns projects/ and external_modules/). Used for cobaya-run and for resolving ROOTDIR-relative emulator roots. null = not set (preflight fails loudly).",
    "driver_root": "The --root value passed to train_single_emulator_cosmic_shear.py (the deploy root that holds the run's dumps + outputs).",
    "driver_fileroot": "The --fileroot value passed to the training driver (the run's file stem).",
    "yaml_dir": "Directory holding train_single_emulator_cosmic_shear.yaml (the golden legs resolve it by bare name), i.e. the repo's example_yamls. Absolute, or relative to rootdir. The gate_configs smoke YAMLs live under gates/configs and are reached from here as ../gates/configs/<name>.yaml (single-yaml_dir limitation, flagged in the harness handoff).",
    "saved_emulator_root": "Path root of an already-saved schema-v2 emulator (<root>.h5 + <root>.emul) for the GSV-C / GCT-C round-trips. null = the save gate trains its own tiny emulator first.",
    "evaluate_yaml": "The cobaya evaluate YAML for GCT-C (defaults to the shipped cobaya_theory/EXAMPLE_EMUL_EVALUATE.yaml; edit its <run> placeholder to the real saved root).",
    "golden_bases": "Per-gate pre-feature commit hash for the byte-identity golden leg, run in a TEMPORARY worktree (never checkout-in-place). GM-C is preset to the pre-EMA commit. A null entry = the gate runs only its functional smoke leg and records the golden leg as skipped (see the harness handoff decision point).",
    "gate_configs": "Per-gate bespoke smoke YAML (relative to yaml_dir). A null entry = the gate reports its config missing and is skipped, naming the file to author."
  },

 "rootdir": "/home/vivianmiranda/extra_data/VivianMiranda/june2026/cocoa/Cocoa",
  "driver_root": "projects/lsst_y1",
  "driver_fileroot": "<your usual --fileroot value>",
  "yaml_dir": "external_modules/code/emulators_code_v2/example_yamls",
  "saved_emulator_root": null,
  "evaluate_yaml": "cobaya_theory/EXAMPLE_EMUL_EVALUATE.yaml",

  "golden_bases": {
    "GM-C": "46ec5e1",
    "GH-E": null,
    "GB-C": null,
    "GL-D": null,
    "GBA-C": null,
    "GME-C": null,
    "GFT-C": null,
    "GHA-F": null,
    "GAN-C": null,
    "GWD-C": null,
    "GPC-C": null
  },

  "gate_configs": {
    "DIAG": "train_single_emulator_cosmic_shear.yaml",
    "GM-D-emasmoke": "../gates/configs/GM-D-emasmoke.yaml",
    "GP-D-single": "../gates/configs/GP-D-single.yaml",
    "GP-D-control": "../gates/configs/GP-D-control.yaml",
    "GH-E-headpatience": "../gates/configs/GH-E-headpatience.yaml",
    "GB-C-headberhu": "../gates/configs/GB-C-headberhu.yaml",
    "GBA-C-anneal": "../gates/configs/GBA-C-anneal.yaml",
    "GME-C-anneal": "../gates/configs/GME-C-anneal.yaml",
    "GFT-C-joint": "../gates/configs/GFT-C-joint.yaml",
    "GFT-C-control": "../gates/configs/GFT-C-control.yaml",
    "GHA-F-pin": "../gates/configs/GHA-F-pin.yaml",
    "GHA-F-license": "../gates/configs/GHA-F-license.yaml",
    "GAN-C-tanh-perfeature": "../gates/configs/GAN-C-tanh-perfeature.yaml",
    "GAN-C-tanh-affine": "../gates/configs/GAN-C-tanh-affine.yaml",
    "GPC-C-residual": "../gates/configs/GPC-C-residual.yaml",
    "GPC-C-ratio": "../gates/configs/GPC-C-ratio.yaml",
    "GPC-C-excl-rescale": "../gates/configs/GPC-C-excl-rescale.yaml",
    "GPC-C-excl-ia": "../gates/configs/GPC-C-excl-ia.yaml",
    "GPC-C-sweep": "../gates/configs/GPC-C-sweep.yaml",
    "item-27-window": "../gates/configs/item-27-window.yaml"
  }
}
